# recipe grammar adversary

This external lane presses the public recipe door with deterministic lawful, malformed, truncated, duplicated, and punctuation-adjacent text captures.

It establishes that bounded text input always produces either one complete bake or one repeatable refusal without a panic or partial output.

The lane also checks that whitespace-only normalization leaves lawful output unchanged.

Text capture cannot manufacture an invisible compiler token group, so the proc-host crossing owns that separate claim.

Coverage-guided search, minimized finding promotion, and campaign resource classification remain the harness fuzz home's work rather than claims of this lane.
