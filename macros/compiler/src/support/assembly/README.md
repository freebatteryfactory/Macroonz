# assembly — one declaration's complete carried delivery

This private home owns the checked join, duplicate-consumption prevention, destination coherence, and the complete support assembly.
It depends on cargo and does not render or deliver the carrier.

An assembly built from a helper holds that helper's identity under the same captured declaration as every carried axis.
The identity enters the assembly's canonical content once, so its movement reaches the carrier plan from the one holder that owns the complete join.
An assembly built from already committed generic cargo names no helper, and its canonical content carries no helper seat.
