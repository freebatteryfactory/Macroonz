# carrier — the inert exported vehicle

This private home owns the exported shell, its plan-keyed name, rendering, and delivery.
It depends on verified assembly and lower cargo but neither proves nor assembles cargo.
The public entrance selects a gate while retaining opaque target input; the internal transcription consumes the binding and table stamp that gate supplies.
The [support boundary](../README.md) owns their coupled crossing and public invocation.
