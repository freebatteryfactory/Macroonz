[package]
name = "macroonz-compiler"
description = "A deterministic code generator as ordinary callable Rust: plan, render, close, explain, and bind one sealed expansion from a complete request."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
host = []

[dependencies]
blake3 = { workspace = true }

[dev-dependencies]
trybuild = { workspace = true }
