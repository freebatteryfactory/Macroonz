[package]
name = "threadpak-macroc"
description = "The metaprogramming services: ordinary callable Rust over the machine's public declaration path."
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
threadpak = { path = "../.." }
