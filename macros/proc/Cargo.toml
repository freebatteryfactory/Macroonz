[package]
name = "macroonz-macros"
description = "Generic trials, bench, and mutations attributes over the macroonz compiler, with the shadow, network, and concurrency declarations beside them."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
proc-macro = true

[lints]
workspace = true

[dependencies]
macroonz = { path = "../compiler", version = "0.0.0", features = ["host"] }

[dev-dependencies]
mh = { package = "macroonz-harness", path = "../../harness" }
