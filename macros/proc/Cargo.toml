[package]
name = "macroonz-macros"
description = "Generic trials, bench, and mutations attributes over the macroonz compiler: each expands to one inert exported carrier beside the item it decorates."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
proc-macro = true

[lints]
workspace = true

[dependencies]
macroonz = { path = "../compiler", version = "0.0.0", features = ["host"] }
