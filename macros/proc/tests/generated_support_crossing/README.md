# Structural carrier crossing

The structural producer fixture calls the public compiler door and emits an inert sealed carrier into a producer library.
A separate consumer invokes that carrier through a renamed root facade and exercises the generated source-selection roads under the harness's actual owner policy.
The complete alternative roster must survive delivery, while a mapped but unpermitted site remains visible and has no executable selections.

These observations establish source custody and selection only.
They establish no execution, activation, equivalence or witness judgment of the generated program.
The compiler's codec and recipe mutation lanes independently compile and judge that program behavior; the harness's mutation lanes own execution qualification and witness outcomes.
The shared proc scratch owner creates the Rust-required producer and consumer boundary from these tracked inputs, and build output uses the repository's ordinary disposable target.
