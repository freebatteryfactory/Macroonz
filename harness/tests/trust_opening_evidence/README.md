# Mutation evidence crossings

These integration claims observe the public mutation, imported-backend custody and trust-composition roads independently of library internals.
Historical artifacts remain paired with their recorded sources and never substitute for a newly executed campaign.

The ignored current-campaign observation reads the caller-supplied `mutation-campaign/` directory under its compile-time Cargo-owned `CARGO_TARGET_TMPDIR`.
It reads no ambient environment override and performs no source discovery.
That directory contains native `console.txt`, one command argument per line in `command.txt`, single-line `version.txt`, `target.txt` and `toolchain.txt`, and the declared source paths in `sources.txt`.
`expected.tsv` has four tab-separated fields per mutant: relative source path, line, column and exact damage description.
`recorded/` contains those source files at their declared relative paths; the current comparison reads the same paths under the harness's repository parent.
Each input file is bounded, missing or malformed material refuses, and declared source paths may not escape their supplied roots.

The existing public backend adapter owns parsing and typed source custody; the campaign observation checks the exact nonempty identity roster and admits only reported witness rejections or compiler-unviable variants.
Unparsed backend text remains visible, and inconclusive materialization is never described as assertion sensitivity.
An equivalence disposition needs separate source-bound justification rather than a permissive survivor filter in this observation.
The runner owns execution and source capture; imported text alone cannot authenticate that a process ran, establish activation or infer a semantic owner.
