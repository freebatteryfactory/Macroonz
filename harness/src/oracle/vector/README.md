# vector — specification-authored bytes

This home reads golden vectors written from a specification and compares one producer result with the exact bytes stated for that input.

The vector pack is independent only when its author did not obtain the expected bytes from the producer under judgement.
Its verdict therefore reaches one stated case and says nothing about inputs the pack does not carry.

The pack reader owns the complete byte grammar, its magnitude-safe traversal, and every refusal that prevents foreign or incomplete bytes from becoming a vector.
The conclusion road translates only this home's verdict into the harness report vocabulary.
