# wrap

This home turns caller-held output from one compiled-mutation backend into a typed reading and a bounded pass plan.

It preserves unparsed material, names the adapter's claim ceiling, and keeps source and profile custody explicit while the caller supplies ownership and operator-family judgments.

It does not execute the backend, establish observed activation, or decide whether a surviving mutation disproves a subject claim.
