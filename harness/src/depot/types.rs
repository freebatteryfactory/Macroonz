//! The declaration-free compatibility bridge over the depot's authored-fact owners.
//!
//! The public type paths remain here while each type's semantic home owns its declaration.

pub use super::operator_family::types::OperatorFamily;
pub use super::type_separation::types::SwapPair;
