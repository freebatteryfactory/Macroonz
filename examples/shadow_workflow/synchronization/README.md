# Synchronization imports

The caller declares its physical Loom binding and the names its increment operations use.
The compiler's [shadow owner](../../../macros/compiler/src/descriptor/shadow/README.md) supplies the configuration-selected imports.
