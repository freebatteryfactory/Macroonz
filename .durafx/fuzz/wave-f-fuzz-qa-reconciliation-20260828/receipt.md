# Wave F fuzz QA reconciliation

This receipt records a local stable Rust 1.98 qualification and awaits owner acceptance and publication authority.

## Authority

- Branch: `codex/macroonz-repository-completion`.
- Tracked evidence authority entering this pass: `1559edb`.
- The owner deliberately synchronized the preceding night work to the remote before this pass.
- This pass performed no push, merge, ref rewrite, hosted-CI activation, or publication.

## Corrected claims

- `CoverageCampaign` joins the declared population, subject revision, coverage interpretation, and closed resource budgets before preflight.
- Active preflight joins that campaign to the actual rustc host triple and the exact rustc and LLVM versions used by the run.
- `RustcProfileResult` joins one internally reserved case ordinal, the exact candidate bytes, the execution classification, the canonical observation, and the qualified campaign standing.
- `CoverageCorpus` consumes that joined result and refuses a reading from another campaign standing.
- `InterestingBytes` has no public constructor and is minted only when a successful joined reading adds a new canonical point.
- A trybuild crossing proves an outside caller cannot mint `InterestingBytes` through its private field.
- Candidate attempts, cumulative candidate bytes, coverage-export bytes, accumulated points, retained cases, and retained bytes all have declared ceilings.
- Admission ceiling refusals leave the coverage frontier and retained corpus unchanged.
- Coverage export reads at most one byte beyond its ceiling, then terminates and reaps the exporter before returning the typed refusal.
- Every execution removes its exact task-created case directory after success or refusal.
- Two separately materialized and separately compiled physical source roots produced the same nonempty canonical observation under one logical root.
- The facade example carries preflight-established target and toolchain facts into the ordinary report road.
- The facade example is a 142-line composition target whose host choreography and example-specific replay specimen live in named support files inside the existing package.
- The fuzz README now states semantic ownership and limits without prescribing a parallel qualification architecture.

## Local qualification

- `cargo +1.98.0 fmt --all --check` passed after formatting.
- `cargo +1.98.0 check -j1 --workspace --all-targets --all-features --locked` passed.
- `cargo +1.98.0 clippy -j1 --workspace --all-targets --all-features --locked -- -D warnings` passed with no suppression.
- `cargo +1.98.0 test -j1 -p macroonz-harness --test fuzz_compose --all-features --locked` passed all 15 crossings.
- `cargo +1.98.0 test -j1 -p macroonz-harness --test compile_refusals --all-features --locked` passed both external refusal lanes, including the new interesting-bytes refusal.
- `cargo +1.98.0 run -j1 --example rustc_coverage --locked` passed through the facade and cleaned its exact run directory.
- `cargo +1.98.0 test -j1 --workspace --all-targets --all-features --locked` passed the complete local workspace wall.
- `git diff --check` passed.
- No task-created fuzz or facade-example run directory remained beneath `target/qualification` after qualification.

## Remaining boundaries

- Cross-host Linux, macOS, Wasm, and hosted CI evidence remains pending its authorized plane.
- Targeted mutation remains a distinct Wave F campaign.
- Package publication order and first registry publication remain human boundaries.
- The exact public diagnostic for an empty `Composition::declared` request remains an owner vocabulary decision.
