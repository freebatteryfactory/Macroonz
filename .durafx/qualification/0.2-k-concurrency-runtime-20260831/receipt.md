# Macroonz 0.2 concurrency and runtime public-package journey

## Accepted observation

This receipt accepts one local Windows stable-Rust journey through the current packaged `macroonz-harness` surface.
One subject-owned safe-Rust consumer composes fault injection, bounded interleaving, temporal judgment, fingerprint-preserving reduction, replay, ordinary runner execution, external-host admission, report standing, and typed infrastructure failure without adding a Macroonz product owner.
The journey closes the selected local interleave and process-host portions of canonical journeys 16.2 and 16.3.
It does not accept all of Wave K, another host, registry-delivered 0.2 artifacts, publication, or Macroonz 0.2.

## Package custody

The consumer uses `macroonz-harness` `0.1.0` from the exact current package archive produced at product source revision `485a4751d397d6dbc6fad7990cda712f8a8bbbd0`.
That archive is 461,573 bytes with SHA-256 `FF579811BA50E0FBEE8F92C8129E71963D6BD3927C7FC1AB74B43280370D5459`.
The archive and its VCS join are already retained by the guarded-transition package receipt and are not duplicated here.
The consumer's only direct dependency is a relative path to its own extraction of that archive.
The copied normalized package manifest received one empty `[workspace]` table so Cargo would not attach the dependency to the containing Macroonz checkout.
That scratch-only workspace boundary changes no package source, metadata, dependency, feature, or product behavior.
The locked graph contains sixteen packages including the consumer, and Cargo metadata resolves the only Macroonz package beneath the consumer's extraction rather than the live checkout or registry.

## Subject-owned schedule claim

The subject owns `Publish` and `Observe`, the publication and observation state, the missing-acquire fault, its stated stale-observation promise, and the invariant that stale observation never occurs.
The quiet reader and the reader carrying the missing-acquire fault enter the same public fault and interleave roads.
The bounded small space contains exactly two schedules and is walked exhaustively.
The missing-acquire subject retains the second enumerated schedule at choices `[1, 0]` as a `PropertyDisagreement` under cause `k-concurrency-runtime/never-observe-stale`.
The quiet reversal exhausts the same two schedules with `SpaceExhaustedAllHold`.
The larger quiet subject has two thirty-two-command strands and an exact counted space of `1,832,624,140,942,590,534` schedules.
Its declared bound samples exactly 4,096 schedules, halts at `CaseBudgetMet`, returns `SampledAllHold`, and produces an equal reading when repeated under root seed `0xA5A55A5AF0F00F0F`.

## Reduction and replay

The canonical counterexample encodes as `[1, 0]` and directly replays the same typed cause and class through the existing interleave interpreter and temporal owner.
The existing `generate::reduction` owner binds that failure to a real refused runner report, a derived probe revision, a generated-support schema identity, the generic chunk-removal-and-zeroing reducer, fingerprint preservation, and a sixteen-probe budget.
Reduction reaches `[1]`, preserves the exact failure fingerprint, and halts at `FixedPointReached`.
The resulting replay capsule carries `ExactDerived` posture and reproduces the same typed cause and class through the interleave interpreter.
Empty material produces no failure, so the reduced witness is non-vacuous for this declared subject without claiming global minimality beyond the reached reduction result.
Direct property evaluation and the interleave wrapper lawfully retain different source locations, so replay equality is asserted at the fingerprint coordinates rather than by flattening those diagnostic sites.

## Runner and process-host standing

The ordinary in-process baseline enters `run_one` and `lens_verdict` as an executed passing trial.
A separate scratch binary writes the exact marker `child-backend-entered` followed by LF, flushes it, and exits with code 23.
The parent observes the non-successful process and records it through `HostTrialRecord` as `RunAttempt::InfrastructureFailed` with `InfrastructureFault::BackendExecutionUnresolved`.
The admitted trial report retains that infrastructure arm, and `lens_verdict` exposes `SeatFailure::HarnessFailed` rather than a subject refusal.
This establishes a typed host-boundary failure crossing without claiming causal crash classification, timeout, resource enforcement, or process-tree cleanup.

## Source custody

The external subject cannot enter Macroonz product authority, while its exact source is necessary to replay this cross-owner composition.
The adjacent `source.tar.zst` is therefore the minimal warranted `.durafx` payload rather than a campaign laboratory.
It contains exactly five files: `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, `src/main.rs`, and `src/bin/infrastructure-child.rs`.
The payload is 6,392 bytes with SHA-256 `CB74DF9BDAE8E8FE142CDF53552390683E8342AD480CB4699A3F25E220BCA19E`.
Two independently created USTAR-plus-Zstandard archives were byte-identical, extraction reproduced every member hash, and the archive contains no reparse point.
The member hashes are:

- `Cargo.toml`, 255 bytes, SHA-256 `8474177F7039A32CB1DB27226368ED3BAA673A98D5CC7EEB47442DB21CB141EB`.
- `Cargo.lock`, 3,280 bytes, SHA-256 `521C6399931F3F0623BFC47936F654FE6E0CBF2421066F21468020CF0506EB90`.
- `rust-toolchain.toml`, 52 bytes, SHA-256 `54231CA6BEA6A491DFA45C5C1E12CC7485AE94C16A374E52933EE1A7CB48248F`.
- `src/main.rs`, 17,391 bytes, SHA-256 `33C4AC2B95110CB596F5C132F64A028A745F070A46A3A167974874B3DA445D74`.
- `src/bin/infrastructure-child.rs`, 354 bytes, SHA-256 `01743869DDC909DF5121CB751C05868AF346C947CADC65ADF28220B76E3B946E`.

Replay materializes the exact harness archive beneath `packages/macroonz-harness-0.1.0`, adds the empty scratch-only `[workspace]` boundary to that copied normalized manifest, and runs the retained consumer with the locked graph.
The package archive, extracted package source, compiled target, logs, and duplicate archive builds do not enter Git.

## Qualification and ceilings

Rustc was `1.98.0 (88d9e12ae 2026-08-18)` with LLVM `22.1.8` on `x86_64-pc-windows-msvc`.
Cargo was `1.98.0 (797e8a9bc 2026-08-05)`.
Stable formatting, locked and offline all-target checking, strict all-target Clippy with warnings denied, all-bin building, and two complete executions passed.
The source contains no unsafe block, nightly requirement, lint suppression, argument-vector control port, print macro, or dependency beyond the packaged harness.
The one exact Cargo target reached 760 files and 408,261,942 bytes before cleanup.
Cargo removed all 760 files, reported 389.3 MiB removed, and the exact target is absent.
The journey explores subject-declared atomic command order only.
It does not execute threads, network delivery, Loom, instruction-level preemption, stable-rustc coverage, memory safety, undefined behavior, stack overflow, timeout, resource limits, descendant cleanup, another architecture, another operating system, or a hosted runner.
The 4,096-case campaign is sampled and makes no exhaustive claim over its larger counted space.
