# Macroonz 0.2 Wave E local shared-substrate receipt

This Git-tracked receipt accepts the bounded local Windows Wave E shared-substrate falsification plane.

## Authority

- Input repository HEAD: `2f03aa2ed103dd4f3b6e70f36f8b35b05ab69be4`.
- Host plane: local Microsoft Windows on `x86_64-pc-windows-msvc`.
- Rust compiler: `rustc 1.98.0 (88d9e12ae 2026-08-18)` with LLVM `22.1.8`.
- Cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`.
- Scratch home: `target/qualification/0.2-wave-e-shared-substrate-20260830`.
- The scratch driver used the current live-worktree `macroonz-harness` path dependency and makes no clean-package or registry-delivered claim.
- No current rustc child process was executed by this scratch witness.

## Source custody

- `source-payload.tar` contains exactly `Cargo.lock`, `Cargo.toml`, `rust-toolchain.toml`, `src/main.rs`, and `result.md`.
- Two independently created USTAR payloads with fixed entry order and modification time were byte-identical.
- Source-payload SHA-256: `42DA8EDD3728F91CD4DAE8F8C36D59A18D2AB6C2BFDC2597ABF276E52F863A61`.
- Source-payload size: 38,912 bytes.
- `Cargo.toml` SHA-256: `CF4F5A2DC91D3868B45428E2D1D2CC8BA11A835911FCF68ADC88D602A76E1D43`.
- `Cargo.lock` SHA-256: `501F0D8FBD90A392705B1AFCED31347CDE539EAB9C474509F35070603A01970F`.
- `rust-toolchain.toml` SHA-256: `3EBDB2005CC3A5B3558C25ABA129043FE701ECCE1F2360B8B6672A97B55E810E`.
- `src/main.rs` SHA-256: `81340AD3471D7EC9F0D3588BB5B966DFC4F1A41B469F630600382CD5C9B3A431`.
- `result.md` SHA-256: `2FB8FF4F6269EE5EBF6260924367BACFCB7D34BC47A85CC6E4C3A1FC6136BAF9`.
- Raw Cargo output remains disposable and has no evidence authority.

## Method provenance

- Lawful compilation and the exact E0308 refusal are reused from `.durafx/qualification/0.2-a-compile-contract-holder-20260829/receipt.md`.
- Parent independently verified that holder receipt's SHA-256 as `B906A55CF527D6A360B81AE49418BDBDA63F5153DA726F07F9E31575A73F5A8C`.
- The accepted holder establishes the real stable-rustc process, exact diagnostic anchor, host standing, and prior runner and report crossing.
- This scratch reconstructs the typed declared and observed compilation values only to test current composition and never relabels them as a fresh process observation.
- The direct-harness temporal precedent is reused from receipt SHA-256 `06B93A748E43880B10D2170EED3F3C63CE6E16FBF05FD5AE1D1803660A810E30`.
- Current temporal complete, incomplete, and hostile readings were freshly executed through `holds_over_drive`.
- The wrong-span compilation observation and compile-prerequisite absence are explicitly planted controls.

## Complete denominator

- One subject-owned table contains seven rows before selection.
- Lawful compile acceptance and exact expected refusal each map to an executed pass through the current compiled-oracle conclusion.
- The planted compile prerequisite absence maps to `Skipped(PrerequisiteAbsent)`.
- The complete temporal drive evaluates two cases, halts at `CaseBudgetMet`, and maps to an executed pass.
- The incomplete temporal drive evaluates one case, halts at `SourceExhausted`, and remains `TemporalDriveStanding::Incomplete`.
- The planted wrong-span compilation declaration maps to an executed `OracleDisagreement` refusal.
- The hostile temporal drive retains a `PropertyDisagreement` counterexample.
- Six conclusion-bearing or unavailable rows have truthful `RunAttempt` values and enter `HostTrialRecord`, `record_one`, and `record_all`.
- The seventh incomplete row remains visibly authored and typed in the seven-row report census as `NotSelected(OutsideSelection)`.
- Selecting all seven rows without forging a seventh attempt refuses exactly as `MissingSelectedRecord`.
- The complete denominator therefore cannot silently omit or relabel the incomplete method result.

## Shared rail and preserved distinctions

- `TrialBinding` carries method and subject identity plus independently derived method and subject revisions.
- `Invocation` carries common bounds, target, toolchain, site, and clock posture.
- `HostTrialRecord` carries only trial identity, truthful attempt, and measurement posture.
- `record_one` and `record_all` derive the execution standing, census, selection, and report custody.
- Compiler error code and primary span remain inside the compiled-oracle values and comparison.
- Temporal generated sequences, halt, evaluated prefix, and counterexample remain inside `TemporalDriveReading`.
- Neither method can forge the other's observation vocabulary through the shared rail.
- No cross-method result union, generic analysis owner, score, or second report engine is needed.

## Exact seam and disposition

- `TemporalDriveStanding::Incomplete` means the method ran but did not earn a conclusion before its declared generation denominator ended.
- No current `RunAttempt` arm can carry that fact without falsely calling it unexecuted, timed out, infrastructure-failed, passed, or refused.
- The method adapter therefore refuses to translate it and retains the typed temporal reading separately.
- This is a truthful method-specific ceiling, not an automatic request to broaden `RunAttempt`.
- The lowest reuse-first disposition is method-specific adapters over the existing runner and report rail.
- Wave E earns no product source, public API, feature, dependency, package, analysis home, process host, evidence schema, or orchestration surface.

## Parent QA and qualification

- The owner agent read the complete assigned authority and authored only disposable scratch material.
- The parent read the complete manifest, lockfile, toolchain file, source, result, current compiled and temporal type vocabularies, and reused holder receipt.
- Parent independently verified both reused receipt hashes and the exact accepted diagnostic anchor.
- Stable Rust formatting passed.
- Stable locked and offline all-target check passed for the exact Windows target with one Cargo job.
- Stable locked and offline strict all-target Clippy passed with `-D warnings` for the exact Windows target with one Cargo job.
- A locked and offline execution passed and emitted fourteen lines.
- Two final direct executions produced byte-identical output.
- Normalized LF UTF-8 output SHA-256: `3BB3CEF2DC90F1FDB32C93C4E060E85F493FF71AB0747278E3274F34F6E64D86`.
- Final binary SHA-256: `DA7AAA3F0A5A1A8776A5E66EAC4A213EB6946CBA185521CEE66F3A26A63B72D2`.

## Ceilings

- This receipt establishes one local Windows x64 shared-substrate composition over current harness source and reused accepted compile-process evidence.
- It establishes no fresh rustc process, package, registry, Linux, macOS, other architecture, hosted, Wasm, performance, publication, or release claim.
- The conclusion-bearing and unavailable rail is reusable as-is.
- Incomplete temporal standing remains method-specific and outside the selected generic attempt rail.
- This receipt accepts the local Wave E plane and does not accept blocked Wave C, later Waves F through N, or Macroonz 0.2.
