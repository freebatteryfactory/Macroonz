# Macroonz 0.2 Wave L derived evidence model

## Standing

This receipt accepts one bounded Git-tree derivation of the current Macroonz evidence account at input repository revision `d81ca1a15daae964ceb28f7d5c12cdcab0e7c465`.
The derivation adds no product source, public API, package, dependency, feature, runtime, qualification crate, Cargo workspace, or semantic authority.
The versioned grammar is `macroonz-evidence-model/1`.
The canonical model has Git content identity `4442dce0811648bde656b21fccab091b1c7af31f`.

## Declared inputs and semantic authority

The retained safe-Rust source states the exact input roster directly: eleven accepted predecessor receipts and nine current live owner files.
Every input is a host-neutral repository path.
The driver refuses an untracked input, a path outside its declared custody class, a working-tree byte that differs from its latest owning commit, a missing section source, a duplicate node identity, or a missing required role family.
The model stores receipt Git blob and owning-commit identities, section line ranges and headings, derived navigation roles, declared and observed package edges, and public module, facade, and proc-macro doors.
It does not copy receipt body sentences or reinterpret an owner value as a second semantic authority.
Independent comparison found zero copied receipt sentences of eighty or more characters and zero personal or absolute-host path hits.

## Model denominator

The model contains twenty sources, eighty-two receipt sections, six architecture edges, forty-three public capability doors, and one hundred fifty-one total nodes.
The six package edges are the exact union of the dependency diagram in `AGENTS.md` and dependency rows in the four product manifests.
Every package edge is both declared and observed: four product edges and two test-only edges.
The role vocabulary is `claim`, `architecture`, `sensitivity`, `cost`, `foundation`, `assumption`, `ceiling`, and the universal `evidence` role.
Roles route readers to authoritative sections and do not claim that keyword classification establishes the truth of a section.

## One model and two renderings

The claims-first assurance rendering and navigation-first evidence graph both name model identity `4442dce0811648bde656b21fccab091b1c7af31f`.
Independent readback found the same sorted one-hundred-fifty-one-node roster in both renderings.
The navigation graph contains twenty source nodes, eighty-two section nodes, eighty-two source-to-section links, four package nodes, and six package links.
A planted model-role mutation moved the canonical identity to `225c235ac168e2b3167a3d60d59cf526ec9c2907` and changed both renderings.
A renderer-title-only change changed presentation bytes while preserving the original model identity.
A planted observed-edge removal produced exact `declared-only` standing and moved the canonical identity to `b353f790031e4b889cc61810f4771e39fe4e76ee`.

## Stable qualification

The one-file driver compiled directly with stable Rust `1.98.0` under warnings denied and unsafe code denied.
The same source passed `rustfmt` and direct `clippy-driver` with `clippy::all` and `clippy::pedantic` denied, with no suppression.
Two complete derivations produced byte-identical files and the same model identity.
Git `hash-object` independently reproduced the identity stated by both renderings from the canonical model bytes.
Every retained text file carries LF only.
The retained source and generated outputs are byte-identical to the independently qualified scratch copies.

## Compact custody

`derive.rs` is 32,584 bytes at SHA-256 `079A9E3621A5FBCEDB473E9D42B7D8F4E671692B0480CB466B55D79F20C442B8`.
`model.tsv` is 18,085 bytes at SHA-256 `21A77AF298CDA690514840DA3091B27F9908C7A8EBA730507601774FD94CDAE2`.
`assurance.md` is 23,597 bytes at SHA-256 `F0EB4F608201A55BC7FEC3EC8774C3CE0E9859AF77667AEBED682B4EC2F7E370`.
`navigation.md` is 17,060 bytes at SHA-256 `6F5141D97320F57C9A2CD1DDF0D4AD6A50B11DAE3BC1F51A67FD326F75A3A55B`.
`validation.tsv` is 456 bytes at SHA-256 `B40BD9B0C9C56AB64D597156422C78408C73F5DCDCF2E51ECA1F88E5F41E7A86`.
No executable, Cargo target, lockfile, package archive, source archive, browser profile, raw log, or duplicate input enters Git with this model.

## Ceilings

The derived roles are deterministic navigation hints, not a claim ontology, assurance standard, aggregate score, adequacy ranking, or automatic release verdict.
The model proves source-to-reference traceability, current Git custody, package-edge liveness, public-door navigation, common rendering identity, and the named controls at this exact Git-tree plane.
It does not prove that every authoritative receipt is semantically correct, that mutable external administration remains current, that every private projection has a public door, or that the Mermaid presentation was browser-rendered on this host.
Future source or receipt movement truthfully moves the model until a new accepted derivation is produced.
