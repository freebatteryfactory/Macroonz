# Macroonz 0.2 compile-contract holder composition

This Git-tracked receipt seals the existing-holder implementation of the generalized compile-contract road.

## Authority and history

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Activation baseline: `7b0a30c2f5bf1211fa44068e2f771dfd543d200c`.
- Composition-census commit: `54d66642e4dcc82ee1d4dc27f553128318c7c726`.
- Holder-composition commit: `7de6a5590c58d6cffdc12f4453102aedea8c316e`.
- Actual-host-standing correction: `e555b29b22c17f1878408907d63a1f8487936d43`.
- Explicit nested-target correction: `98122b2076e8037cc35c8b60001bbb86d5a43d27`.
- This lot authorizes no push, merge, ref rewrite, publication, registry mutation, nightly toolchain, or product-source movement.

## Ownership result

Reuse-ladder disposition 1 remains correct: composition is complete on the existing public owners.

- The compiler and proc host generate and carry the real network projection.
- `harness::oracle::compiled` owns exact compilation declarations, observations, comparison, and conclusions.
- The external `compile_refusals` holder owns Cargo and rustc effects, structured diagnostic capture, physical-path normalization, and package-shaped subjects.
- `HostTrialRecord`, `record_one`, and `record_all` admit those conclusions under binding-derived runner and report standing.
- The facade routes the generated consumer through the renamed dependency `bakery` and the existing `harness` feature.

No product source, public API, package, dependency, feature, manifest, lockfile, facade edge, failure class, identity grammar, or diagnostic family changed.

## Retained external crossing

The existing `compile_refusals` integration target now carries four real external subjects.

- The handwritten lawful source compiles.
- The handwritten hostile source refuses with exactly one relevant primary E0308 anchor at `src/bin/handwritten-hostile.rs:5:13..5:20`.
- The facade-generated lawful source expands `bakery::macros::network!`, reads the generated topology and schedule, and compiles.
- The facade-generated hostile source refuses with exactly one relevant primary E0308 anchor at `src/bin/generated-hostile.rs:14:13..14:21`.
- Generated source never grades itself; the external host alone constructs `ObservedCompilation` and compares it with the independent declaration.
- A wrong-code control reaches cause `compiled-diagnostic-error-code`.
- A wrong-span control reaches cause `compiled-diagnostic-primary-span`.

All six conclusions cross `RunAttempt`, `HostTrialRecord`, `record_one`, and `record_all`.
The public report readers recover the exact derived subject revision, check revision, target, toolchain, `ReplayPosture::ExactDerived`, failure class, and oracle cause.

## Host standing

The retained host runs `cargo +1.98.0 --version --verbose` and `rustc +1.98.0 -vV` once before compiling any subject.
It requires both release fields to equal `1.98.0`, requires both nonempty host fields to agree, and retains both exact first identity lines.
It passes that captured host explicitly as every nested Cargo check's `--target`, so ambient Cargo target selection cannot disagree with the target recorded in the report.
The host facts enter only target and toolchain standing and never diagnostic identity or the compilation verdict.

This Windows run established:

- Target: `x86_64-pc-windows-msvc`.
- Cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`.
- Rustc: `rustc 1.98.0 (88d9e12ae 2026-08-18)`.
- Runner toolchain spelling: `cargo 1.98.0 (797e8a9bc 2026-08-05); rustc 1.98.0 (88d9e12ae 2026-08-18)`.

Unavailable or wrong-release tooling and a Cargo/rustc host mismatch remain typed prerequisite absence.
Spawn and capture failures remain infrastructure.

## Stable holder qualification

All Cargo work was serial and used the stable Rust 1.98 toolchain.

- `cargo +1.98.0 fmt --all -- --check` passed.
- `cargo +1.98.0 test -p macroonz-harness --test compile_refusals --locked --offline -- --test-threads=1` passed 8 of 8 tests, including all 30 compile-fail fixtures.
- `cargo +1.98.0 clippy -p macroonz-harness --test compile_refusals --locked --offline -- -D warnings` passed.
- The coordinator independently reran the serialized target at final code commit `98122b2076e8037cc35c8b60001bbb86d5a43d27`; it passed 8 of 8 tests and all 30 compile-fail fixtures in 76.80 seconds.
- `git diff --check` passed across each holder commit.
- The changed-file scan found no product source, manifest, lockfile, `unsafe`, `#[allow]`, `#[expect]`, nightly, Frida, LibAFL, or new qualification package.

## Clean package candidates

The final package proof was regenerated from the clean exact code commit `98122b2076e8037cc35c8b60001bbb86d5a43d27`.
Each package command was `cargo +1.98.0 package -p <package> --locked --offline --no-verify` with one build job and no `--allow-dirty`.
Every unpacked `.cargo_vcs_info.json` names the full exact commit and carries no dirty field.

- `macroonz-0.1.0.crate`: 128,356 bytes, 20 entries, archive SHA-256 `4E5E790DAFB8735180D2F9659DCE55B2B02F76F038D627715FEDF609770BE80C`, LF roster SHA-256 `93025489F337589DC0BE9E15A6F633A64D2DAA0B67DA5F88EA315C2DBADF62CD`.
- `macroonz-compiler-0.1.0.crate`: 393,903 bytes, 305 entries, archive SHA-256 `E323E9BE002F9BDF8673217D5D9922453891AB509F630D092B620B35272D443E`, LF roster SHA-256 `BA30EF5AD1D9A17CF2AB8639E50B4B2967FDFCBF9E2527491A53A82DB667E4EF`.
- `macroonz-harness-0.1.0.crate`: 454,251 bytes, 373 entries, archive SHA-256 `13F4C215C3BE5E83B0B8CAE4A6FBAC227734AAF065A87FCE959CCA28514830EB`, LF roster SHA-256 `A2022C9E3F4317AB87EFD3868B03E5F8D5A99335532C490FB8E8C6555D573329`.
- `macroonz-macros-0.1.0.crate`: 20,824 bytes, 32 entries, archive SHA-256 `741148ECAED4447C36FDE14B172CAFB8D11C2A4A2FA84C18249567F326CF8A14`, LF roster SHA-256 `594D4DB33D97BA10AFBDB6E8BC902295D17350E3915CEA6BF8073FA9A95F11E1`.

The coordinator independently recomputed every archive hash, entry count, and LF roster hash and read every unpacked VCS record.

## Package-only adopter matrix

The disposable adopter owns its own `[workspace]`, has one member, and declares one direct dependency: `macroonz` renamed `bakery`, with default features disabled.
Its root-only `[patch.crates-io]` table points to the four unchanged unpacked candidates.
Independent Cargo metadata confirmed four of four Macroonz manifests beneath the final unpacked directory and zero live-workspace manifest paths.

On native `x86_64-pc-windows-msvc`:

- Handwritten and generated lawful subjects compiled with the facade `harness` feature.
- The handwritten hostile subject refused with exactly one E0308 anchor at `5:13..5:20`.
- The generated hostile subject refused with exactly one E0308 anchor at `14:13..14:21`.
- Without the `harness` feature, metadata resolved no `macroonz-harness`, and the neutral facade/compiler reachability subject compiled.
- A separate harness-enabled qualification host consumed those two measured no-harness facts and recorded `SkippedWithReason(PrerequisiteAbsent)` with no subject verdict.
- The coordinator independently reran the generated lawful package candidate and it compiled from the unpacked archives.

On `wasm32-unknown-unknown`:

- Both lawful subjects passed compile-only checks.
- Both hostile subjects produced their same single exact E0308 anchors.
- No Wasm subject was executed.

## Scratch custody

The accepted package proof lives temporarily at `target/qualification/0.2-a-compile-contract-holder-20260829/final`.
After the coordinator's independent checks, the final subtree contained 2,224 files totaling 985,771,472 bytes.
The complete task-owned target, including superseded pre-commit and pre-correction observations, contained 6,151 files totaling 2,834,108,982 bytes.
Those superseded observations carry no accepted authority.
The exact task target may be deleted only after this receipt is committed and independently verified.

## Ceiling and next obligation

This receipt establishes the generalized compile-contract holder on local Windows, local clean package candidates, native package execution, and Wasm compile-only package behavior.
The packages still carry version `0.1.0`; this is not registry-delivered 0.2 evidence.
Linux, macOS, hosted, registry-delivered, and runtime Wasm standings remain unproved.
The package commands used `--no-verify`; the separately patched adopter rather than Cargo's package verifier exercised the declared consumer matrix.
No accepted claim depends on the superseded dirty archive run.

The next Section A obligation is the separate targeted stable `cargo-mutants` campaign through the existing `CompiledSuitePressure` custody holder.
An expected compiler refusal remains a compiler-contract result and must not be relabeled as successful compiled-specimen pressure.
