# 0.2 Wave B subject-owned version/evolution pilot

## Accepted plane

This receipt accepts one local Windows stable-Rust subject-repository observation against the current Macroonz 0.2 campaign package surface.
It establishes that one subject-owned proc macro can compose the existing compiler, facade, harness, compiled-oracle, runner, and report batteries into a closed version graph with exact adapter accounting and external compatibility evidence.
It does not close Wave B, cross-subject pressure, multi-family composition, mutation, scaling, cross-host qualification, or Macroonz 0.2 acceptance.

## Repository authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Product baseline: `3a119d7d015e337e2703fa811b9bb5fb8e94c391`.
- The tracked product worktree and index remained clean throughout the scratch run and independent parent review.
- The scratch workspace resolved no Macroonz package from the live repository tree or the registry.
- This observation earned no product source edit, public API, owner, identity grammar, feature, dependency, package, facade edge, relation primitive, impact home, version ontology, first-party proc entry, runtime interpreter, or qualification architecture.

## Subject topology and ownership

The disposable workspace contains exactly three subject packages:

- `evolution-subject-macros`, which exists because Rust requires a proc-macro package boundary and owns the private subject compiler engine;
- `evolution-subject`, which owns the ordinary public evolution contract, macro invocations, generated roads, and independent handwritten road;
- `evolution-challenges`, which exists only to observe the public roads from an external package boundary and owns process execution, compiled-oracle, property, runner, and report claims.

The proc library retains only compiler-host capture, selection of one private subject compiler road, diagnostic placement, and emission.
The subject engine owns the version and edge grammar, forward-only law, adapter completeness, canonical content, rendering, and hostile declaration controls.
The harness owns no version, graph, edge, adapter, or migration semantics.
The compiler owns no subject policy and receives only the declaration values required to produce the subject's requested output.

## Subject contract

- The subject declares the closed version roster `V1`, `V2`, and `V3`.
- The baseline graph admits `V1 -> V2` through `V1ToV2` and `V2 -> V3` through `V2ToV3`.
- The baseline graph has no `EvolveTo<V3>` implementation for generated `V1`.
- Every written lawful edge receives exactly one adapter seat before an informed declaration exists.
- Foreign, duplicate, self, and backward edge rows are refused under this subject's law.
- Missing, doubled, foreign, and reused adapter seats are refused.
- Generated, alternate-renderer, and handwritten roads use the same ordinary subject-owned `EvolveTo<Next>` contract.
- Public constants expose the complete version and edge rosters as ordinary Rust values.
- The handwritten implementation repeats its adapter behavior independently while an explicit shared-substrate roster names the common public contract, vocabulary, arithmetic declaration, and package revision.

## Existing batteries composed

The subject composes current `TextCapture`, `CapturedInput`, `Bounded`, `CanonicalContent`, `Kind`, `SoleRole`, `NoQuestions`, request, plan, render, closure, explanation, expansion, identity, host, and generated-token surfaces.
The external challenge composes current parity, fused-versus-separate, lawful-admission, fail-closed, compiled-readback, compiled-refusal, runner, report, revision, target, toolchain, and replay-posture surfaces.
No codec, stamp, support carrier, temporal contract, banked relation candidate, banked impact candidate, or new harness lane was forced into this family.

## Independent behavior and hostile controls

The compiler-side external target passed nine tests.
The baseline declaration produces exact version and adapter rosters and omits a direct `V1 -> V3` edge.
Empty, duplicate, and overbound version rosters are refused.
Foreign, duplicate, self, and backward edges are refused.
Missing, doubled, foreign, and reused adapter seats are refused.
A malformed arrow produces a typed subject issue at the actual wrong token.
Adding the declared `V1 -> V3` edge and adapter moves raw capture, canonical content, plan, generated-unit key, generated bytes, closure, explanation, expansion, and complete edge readback.
Permuting set-semantic edge and adapter clauses preserves canonical content and generated bytes while raw-capture-anchored plan and expansion identities truthfully move.
Closure refuses both an omitted graph unit and a doubled graph unit.

Parent QA rejected the first private-refactor witness because it cloned the declaration and called the same renderer.
The accepted repair contains separately authored adapter trait, result type, `adapt`, `evolve`, and implementation token assembly operations that never call the direct adapter or method renderers.
The two renderer routes share only the declared subject semantics, low-level token primitives, non-adapter structure, and roster rendering.
Under identical captured input, the direct and alternate routes preserve exact plan, closure, explanation, expansion, and generated-byte identity.

The external challenge passed one effectful journey with fifteen complete report rows.
Four compiled processes establish direct-generated, alternate-generated, handwritten, and planted readback.
The direct-generated, alternate-generated, and handwritten roads each transform input `7` through `V1 -> V2 -> V3` into `19` and expose identical baseline rosters.
The compiled oracle accepts all three exact baseline readbacks.
Harness parity accepts direct-generated versus alternate-generated behavior and direct-generated versus handwritten behavior under the explicit shared-substrate ceiling.
Adding the planted direct `V1 -> V3` adapter preserves the unchanged two-edge behavior, and direct versus composed migration both produce `19`.
A planted wrong adapter produces `20` and is caught outside the generator.
The baseline public membership reading admits `V1 -> V2` and fail-closes on unwritten `V1 -> V3`.
Stable rustc refuses the external direct-edge challenge as `E0277` at `evolution-challenges/src/bin/direct_edge.rs:11:10-11:42`.
Stable rustc refuses the macro-removed challenge as `E0425` at `evolution-challenges/src/bin/macro_removed.rs:7:17-7:28`.
Wrong readback, wrong diagnostic code, and wrong diagnostic span controls each produce the expected typed disagreement.
Every conclusion passes through `record_one`, and the complete fifteen-row denominator passes through `record_all` with exact derived revisions, target, toolchain, and `ReplayPosture::ExactDerived`.
Constructed property findings remain identified as constructed observations and are not promoted into host-execution claims merely by entering the report vocabulary.

## Package-only crossing

Fresh `.crate` candidates for all four Macroonz packages were created from the clean baseline and extracted beneath disposable scratch.
Every extracted candidate cites baseline `3a119d7d015e337e2703fa811b9bb5fb8e94c391` with its truthful package-relative source path.
Locked and offline metadata names exactly the three scratch subject packages as workspace members.
Every Macroonz manifest resolves beneath the extracted scratch package tree.
The proc carrier and external challenge consume Macroonz only through the `macroonz` facade renamed to `bakery`, with the harness feature enabled only in the challenge.
The ordinary subject consumes only its required sibling proc package.

The package archives were:

- `macroonz-0.1.0.crate`, SHA-256 `13673940C98194326C9B31316458DA9AAC5C50CFFE9CBD9F7B0095F3E81BF17F`;
- `macroonz-compiler-0.1.0.crate`, SHA-256 `C62A39B9821BD0A04D9FDB4C3549D9FE1DEAF413449E269ECEDAEF95B77A44DD`;
- `macroonz-harness-0.1.0.crate`, SHA-256 `AE5BBCF767F1BEAE0F08E493AF59D6085D0876504872B6A888EEB095BFB8383B`;
- `macroonz-macros-0.1.0.crate`, SHA-256 `298AB4FDDE2D0E46EE871356B25136BE3AA2F38BD1DC9DC9D9DC1315FAF988A9`.

## Stable host and parent wall

- Operating system: Microsoft Windows 11 Home, version `10.0.26200`, build `26200`, 64-bit.
- Rust target and host: `x86_64-pc-windows-msvc`.
- Rust: `rustc 1.98.0 (88d9e12ae 2026-08-18)`.
- Cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`.
- LLVM: 22.1.8.
- Cargo execution after package preparation: serial, locked, offline, and scratch-targeted.
- Each spawned process or compiler challenge carries an exact 60-second ceiling and a Windows process-tree termination path.
- Wall measurement is explicitly unavailable.

The independently rerun parent denominator passed:

- stable 1.98 formatting;
- the serial locked and offline all-target workspace suite with nine engine claims and one effectful external journey;
- all-target check and build;
- workspace documentation without dependencies;
- strict all-target Clippy with warnings denied;
- full locked and offline metadata plus dependency-tree inspection;
- exact package-candidate hash and `.cargo_vcs_info.json` inspection;
- raw runtime output and exact stable diagnostic inspection;
- 18-of-18 retained-source hash and LF checks;
- payload list and independent extraction with 18-of-18 content hashes;
- source-law, private-identity, absolute-path, build-script, reparse-point, and live-workspace-fallback audits;
- final tracked worktree, index, branch, and baseline verification.

The authored source contains no library `#[cfg(test)]`, `#[allow]`, `#[expect]`, unsafe operation, personal identity, absolute user path, build script, or reparse point.

## Exact source custody

`source-payload.tar` retains only the root scratch manifests, lockfile, toolchain declaration, and complete authored source and external tests of the three subject packages.
It contains no extracted Macroonz package, package archive, compiled target, runtime output, Cargo JSON, reference copy, absolute user path, or general build output.
Two independent payload creations produced the same 119,296-byte archive with SHA-256 `315AD3FE98F5F5945E0BF72F3D9A2B576FC78D4C4980416B9A38F0908434E518`.
Parent extraction reproduced all 18 retained files byte for byte.
The scratch qualification record had SHA-256 `00C04AF83D493BEFC48387C570BA87DAC0D8DB005E21C19703C48BD68D910BCA` before sealing.

## Disposition

The version/evolution subject reaches reuse-ladder disposition 1: existing composition is complete for this bounded behavior.
Current compiler and harness batteries already supply the necessary mechanics while the subject retains every version, edge, adapter, compatibility, and migration choice.
The banked generic relation candidate remains unearned because this homogeneous evolution graph contradicts its assumed bipartite rosters and does not authorize Macroonz to choose an ontology.
The banked impact candidate remains unearned until the program's independent multi-subject pressure denominator exists.
This pilot accepts only the local Windows version/evolution family crossing.
After this receipt and payload are committed and verified, the exact disposable `target/qualification/0.2-wave-b-version-evolution` tree and the parent payload-verification tree may be cleaned when no active task depends on them.
