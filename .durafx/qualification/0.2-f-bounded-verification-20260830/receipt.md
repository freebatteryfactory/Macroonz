# Macroonz 0.2 Wave F local bounded-verification receipt

This Git-tracked receipt accepts the bounded local Windows semantic half of Wave F.

## Authority

- Input repository HEAD: `c7766b73be999aef09c49f83d7b36aaa18bca134`.
- Host plane: local Microsoft Windows on `x86_64-pc-windows-msvc`.
- Rust compiler: `rustc 1.98.0 (88d9e12ae 2026-08-18)` with LLVM `22.1.8`.
- Cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`.
- Scratch home: `target/qualification/0.2-wave-f-bounded-verification-20260830`.
- The scratch driver used the current live-worktree `macroonz-harness` path dependency and makes no clean-package or registry-delivered claim.
- The scratch workspace contains one unpublished package and no product, workspace, dependency, feature, or facade change.

## Source custody

- `source-payload.tar` contains exactly `Cargo.lock`, `Cargo.toml`, `rust-toolchain.toml`, `src/lib.rs`, `src/main.rs`, `tests/bounded.rs`, and `result.md`.
- Two independently created USTAR payloads with fixed entry order and modification time were byte-identical.
- Source-payload SHA-256: `7E133CF4ED05EC064AFC03FA6AB757D17B840460B58E0604ED397CD6989A4457`.
- Source-payload size: 49,664 bytes.
- `Cargo.lock` SHA-256: `294ECCD45C08EED79D6E7D447DDD0BAD37708F090097D9D4AC3922AE0777F6D1`.
- `Cargo.toml` SHA-256: `65088A5FE5DCA9F30ABE5F110598A8C8A45F8721B06D7D4A46E170DC227EC24F`.
- `rust-toolchain.toml` SHA-256: `E879701472A698533F43F54989BBF5F3101EB0503472159D32633F3BB8A13FDF`.
- `src/lib.rs` SHA-256: `3A22884F05427B354E3A242A1E4C3C57FE98C0354B809D71D84B96BF22A40F5E`.
- `src/main.rs` SHA-256: `9075948EAE5D9A2E33EFF9D1195B3C96D8EF03A55F54D3ECF6ACD4F87610511A`.
- `tests/bounded.rs` SHA-256: `2DDD9C336A69992C09ECE5315763CE430913773EC62ED76CB35E6C3EA342C869`.
- `result.md` SHA-256: `AEC8F50C7105610BCEA5D1C2B29ACDC4A5C4BAF439065AD33F8FE0CB10D6A994`.
- Independent extraction reproduced every retained file hash.
- Raw Cargo output remains disposable and has no evidence authority.

## Complete declared denominator

- The subject vocabulary is the inclusive `u8::MIN..=u8::MAX` iterator collected into supplied bytes.
- The denominator is derived by counting that same iterator and is not a separately authored cardinality constant.
- The resulting denominator is 256 one-byte members in supplied order.
- Current `GenerationPlan`, `InputOrigin::Supplied`, `ByteSource`, constant `CaseWidth`, `TransitionContract`, and `holds_over_drive` owners perform the bounded exploration.
- The lawful rotate-and-XOR operation and its inverse passed all 256 members.
- Every retained sequence was exactly one input byte and one decoded member, every ordinal matched its supplied position, and independent set accounting found no duplicate or omitted member.
- Completion was admitted only under `CaseBudgetMet`, a generated census of 256, 256 evaluated members, and a `Passed` temporal conclusion.

## Controls and counterexample

- The lawful `InvertV1` variation preserved the roundtrip property over the reversed complete denominator while retaining its distinct method and ordering facts.
- A one-member-short supplied source observed `SourceExhausted`, 255 generated members, 255 evaluated members, and `TemporalDriveStanding::Incomplete`.
- A one-member-tight byte budget observed `ByteBudgetExhausted`, 255 generated members, 255 evaluated members, and `TemporalDriveStanding::Incomplete`.
- A claim-free contract refused exactly as `NoClaimDeclared`.
- `InfrastructureFailed(GenerationUnavailable)` is an intentionally constructed unavailable posture and is not an observed host failure.
- The intentionally planted inverse defect at `0xA5` produced `PropertyDisagreement` with the declared `wave-f-bounded-verification/byte-roundtrip` cause at zero-based position 165 after 166 evaluations.
- Generation still retained and independently inspected the complete 256-member denominator before temporal evaluation stopped at the first counterexample.
- The counterexample crossed current `HostTrialRecord`, `record_one`, generic byte reduction, and replay-capsule owners.
- The capture-free reduction probe rederived the report fingerprint before reduction, and a reconstruction failure would refuse qualification rather than become a no-failure observation.
- Empty and zeroed candidates both produced `NoFailure`, no shrink was accepted, no fingerprint moved, and reduction halted at `FixedPointReached`.
- The replay capsule retained exactly `0xA5`, the refused execution key, and `ReplayPosture::ExactDerived`.

## Parent QA and qualification

- The owner agent read the assigned program, portable context, current generation, temporal-property, report, reduction, replay, and descriptor authority before authorship.
- The parent read the complete final manifest, lockfile, toolchain file, library, binary, external tests, and scratch result before acceptance.
- The isolated manifest carries the repository warnings-denied Rust wall and the complete `all` plus `pedantic` Clippy wall without a suppression.
- Stable Rust formatting passed.
- Stable locked and offline all-target check passed for the exact Windows target with one Cargo job.
- Stable locked and offline all-target tests passed with two external integration tests and no failure.
- Stable locked and offline all-target Clippy passed under the manifest-declared repository wall with one Cargo job.
- Cargo metadata confirmed one isolated unpublished scratch member, the current harness path dependency, no feature, and a scratch-local target directory.
- Two direct executions produced byte-identical 14-line, 929-byte LF UTF-8 output.
- Output SHA-256: `1D47AA7C63C433426FEC4FBCCD813A819A5D4AE3BEFCB70566F752FD544CD587`.
- Executed binary SHA-256: `A528320140FDDB261D7FC2EB1C6C06723C33DB85048999F39C3D8966EDB785D4`.
- The retained source has LF line endings, no byte-order mark, and no reparse point.
- The retained source contains no unsafe operation, `#[allow]`, `#[expect]`, library `#[cfg(test)]` item, authored build script, personal identity, or absolute user path.

## Disposition and ceilings

- Existing generation, property, report, reduction, and replay owners are sufficient for this bounded semantic composition.
- The banked standalone `FiniteDomain` product type remains rejected because the existing composition supplies the required behavior without another owner or ontology.
- The exact source exposes an ordinary scratch library and external test target so the next sublot can apply targeted mutation to exercised code.
- This receipt establishes exhaustive exploration only within the declared one-byte domain and declared roundtrip property.
- It is not symbolic proof, a universal theorem, registry-delivered qualification, package qualification, a Linux or macOS observation, another architecture, hosted evidence, global-wall evidence, or human acceptance.
- The planted unavailable posture is not an observed host failure, and the planted `0xA5` defect is not a discovered product defect.
- This receipt accepts the local Wave F semantic half and leaves targeted Muterprater sensitivity, complete Wave F acceptance, blocked Wave C, later waves, cross-host work, and Macroonz 0.2 acceptance open.
