# Merged-main local sanity receipt

This Git-tracked receipt records the fresh-branch local sanity pass over the synchronized merged `main` tree.

It does not activate hosted CI, governance, publication, or another host plane.

## Source and integration custody

- Sanity branch: `codex/macroonz-sanity`.
- Merged-main base: `b7ce052b3e07455bd15512f0bd8f1fe948988f2f`.
- Repository-specification correction: `44c5e16fb78f428c6b341a2979318de005517906`.
- The merge has exactly the parents `755e4d8fec6a16391b768bbf35e461404296fbfd` and `773544e10b966c182d722c3aeddca960cdac8040`.
- The merge tree is byte-identical to the second parent's tree, so the merge introduced no manual resolution content.
- The complete `report` home and Muterprater home remain unchanged from accepted snapshot `8c184279189b83e5a94d0cba3108ac561227bd87`.
- The five `FailureClass` rows and the Muterprater `wrap` custody road therefore remain byte-identical to their accepted seats.

## Repository-specification audit

- Every one of the 74 README files was read whole as a product, package, or semantic-home specification.
- The current README denominator is 3,141 physical source lines and 198,837 bytes.
- The harness overview now accounts for the public interleave, network, and target-qualified preemption instruments.
- Compiler-home README file maps were replaced with durable ownership, behavior, and nonclaim language.
- Public Muterprater operation paths remain stated while private file and mount mechanics no longer form a second specification.
- The root product README points to one contributor-owned local-wall surface instead of duplicating a partial command list.
- Every README filesystem link resolves, no prose uses four-space Markdown indentation outside a fence, and the changed prose obeys the sentence-per-source-line law.
- The tracked identity search is empty for `eassa`, `heyoub`, `eayou`, `C:\Users`, `Downloads`, and `/mnt/c` after two host-location phrases were made neutral.
- The repository still contains sixteen Mermaid blocks, each with an accessibility title and description.
- Every Mermaid block is byte-identical to the merged-main base, so the prior hosted visual observation at `.durafx/qualification/wave-f-github-mermaid-20260828/receipt.md` remains the applicable rendering receipt.

## Architecture and dependency audit

- The root workspace contains exactly `macroonz`, `macroonz-compiler`, `macroonz-harness`, and `macroonz-macros` at version `0.1.0` and Rust version `1.98.0`.
- The only Cargo project outside that product workspace is the narrow compiler-required proc-macro observer fixture.
- No general-purpose qualification package, `build.rs`, tracked symlink, `#[allow]`, `#[expect]`, or Macroonz-owned unsafe block exists.
- Exact pins remain `arbitrary = 1.4.2`, `blake3 = 1.8.6`, `loom = 0.7.2`, `ra-ap-rustc_lexer = 0.174.0`, `syn = 3.0.3`, and `trybuild = 1.0.120`.
- Every repository commit cited by a tracked receipt resolves in current `main` ancestry.
- The two receipt hashes that do not resolve as repository commits are explicitly labeled compiler and Cargo toolchain commit identities rather than repository revisions.

## Composition cure closure

- Empty composition refuses as provider absence, provider overflow retains its exact magnitude, and duplicate identities retain each provider identity once.
- Magnitude is settled before duplicate work, and the admitted provider ceiling bounds the complete duplicate-finding denominator without a Composition-specific truncation surface.
- Exact independent canonical vectors cover absence, overflow, and two identity-bearing duplicate issues.
- Absence, overflow, and duplicate findings each project their issue-specific repair through the existing typed diagnostic system.
- The outside compile refusal proves that a caller cannot mint a `CompositionError` beside its guard.
- The retained 41-mutant feedback campaign remains closed with twenty caught mutants, twenty-one unviable mutants, zero viable misses, and zero timeouts.

## Focused crossings

- `cargo +1.98.0 test -j1 -p macroonz-compiler --test descriptor_content_receipts --locked` passed 10 of 10 tests.
- `cargo +1.98.0 test -j1 -p macroonz-compiler --test compile_refusals --locked` passed its complete 27-fixture refusal roster.
- `cargo +1.98.0 test -j1 -p macroonz-harness --test fuzz_compose --all-features --locked` passed 18 of 18 tests.
- `cargo +1.98.0 test -j1 --test facade_harness_surface --all-features --locked` passed its facade composition crossing.

## Stable Rust 1.98 local wall

- Formatting passed.
- Locked serial workspace check passed across every target and feature.
- Locked serial Clippy passed across every target and feature with warnings denied.
- Nextest passed 423 of 423 executed tests across 60 binaries, with nine intentional child fixtures skipped by direct enumeration and exercised through their owners.
- Workspace doctests passed 4 of 4 executed examples.
- Cargo deny passed advisories, bans, licenses, and sources.
- Locked serial rustdoc passed with warnings denied.
- Locked serial `wasm32-unknown-unknown` checking passed with every feature enabled.
- The facade `rustc_coverage` example compiled and executed successfully.

## Package and adopter observations

| Package | Entries | LF-roster SHA-256 |
| --- | ---: | --- |
| `macroonz-compiler` | 302 | `94B470461099080C3AC0310ACEC691C739C9DBA4AF102EE1DC1D6240F43CEBE8` |
| `macroonz-harness` | 364 | `D0386A63F2317BE55E52C4FEF2186754D3651D4F48467F1205E0142EF9EFE087` |
| `macroonz-macros` | 30 | `B8233E6B4A0E6035B34CD99D20A074FD282C87FAE28A5376373A6AF435D37B10` |
| `macroonz` | 19 | `95B3D6C59DA4539720C91118FE765DC17DFAB77137EC3E102792E7DD2E4EAF05` |

- The compiler roster's two-entry growth from the prior receipt is exactly the retained Composition compile-refusal source and stderr witness.
- The proc-macro archive roster excludes the compiler-boundary observer fixture as its repository-test role requires.
- The facade archive roster retains the complete rustc-coverage example road.
- The committed compiler snapshot packaged and verified offline as 302 files and a 388,610-byte archive.
- The compiler archive SHA-256 is `1B258C6D8C6A5E35A269E5EF695320E2824BFD5B0C80A21AD9A1A8A6C1181AFC`.
- Disposable path adopters generated locked offline graphs, compiled, and executed successfully in default-full, diet-lite, diet, and renamed-dependency postures.
- The first disposable adopter preflight refused because a nested scratch project had not declared its own workspace boundary; adding the empty scratch-only workspace table repaired that test topology without changing product source.

## Scratch custody before cleanup

- The compiler-package scratch is `target/qualification/wave-f-main-sanity-package-20260828` with 695 files and 277,163,108 bytes.
- The adopter scratch is `target/qualification/wave-f-main-sanity-adopters-20260828` with 1,323 files and 829,120,632 bytes.
- These exact task-created directories remain until this receipt is committed.
- Neither scratch directory has evidence authority after the observations above are retained here.

## Remaining planes

- Wave F remains active rather than globally complete.
- Hosted security and CI, physical Linux, cloud Linux, macOS, ARM64, dependency-ordered publication, registry retrieval, as-delivered qualification, and final owner acceptance remain unexecuted.
- Stable branch and MC/DC coverage remain unavailable under the authorized stable Rust 1.98 denominator and were not replaced by a floating nightly run.
- The retained mutation, long-schedule, performance, coverage, package, and hosted-Mermaid receipts were reconciled rather than rerun without a new contradiction.
- No push, merge, remote-branch deletion, workflow activation, governance mutation, crate publication, registry mutation, or release operation occurred in this sanity pass.
