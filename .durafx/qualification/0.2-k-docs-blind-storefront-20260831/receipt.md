# Macroonz 0.2 docs-blind storefront journey

## Accepted observation

This receipt accepts one local Windows stable-Rust docs-blind journey through the current packaged compiler and harness surfaces.
A fresh actor received only the extracted public package candidates, their packaged READMEs, public source documentation, and packaged examples.
The actor authored one standalone consumer that completed a useful direct handwritten temporal property and a useful callable compiler request without reading the live repository source, internal tests, working law, completion program, portable context, prior receipts, or other qualification scratch.
This observation retains the external actor and package-only boundary that the executable examples cannot observe themselves.
It does not accept all of Wave K, another host, hosted execution, registry-delivered 0.2 artifacts, publication, or Macroonz 0.2.

## Public source boundary

The actor used these packaged public owners:

- the facade, compiler, and harness READMEs;
- `macroonz-harness-0.1.0/examples/temporal_property.rs` at SHA-256 `291E7AFB7D8E6DF30F8689D430141787FE71F93DCAC759EF98B15A0CF2B43997`;
- `macroonz-compiler-0.1.0/examples/callable_compiler.rs` at SHA-256 `9138414639184669E589152C0ADF1128C234FD6269B035ADF560D4434DD2FBD1`;
- the packaged compiler and harness public rustdoc source where the examples required exact item signatures.

The actor did not inspect a live product source file or an internal test.
The standalone manifest owns its own empty workspace boundary and depends only on relative paths to the two extracted package candidates.
Cargo metadata resolved `macroonz-compiler` and `macroonz-harness` only beneath the package extraction named by the accepted guarded-transition package receipt.
No Macroonz dependency resolved from the live checkout or registry.

## Completed journeys

The direct harness journey owns its connection state, connection events, transition function, capacity invariant, lawful history, and hostile over-capacity history.
The harness supplies only the temporal contract, whole-history reading, typed finding cause, and typed conclusion.
The callable compiler journey owns its `RetryBudget` kind, compiler door, declared posture, retry-value choice, and requested constant projection.
The compiler supplies only captured input, the request road, planning, rendering, closure, explanation, binding, and emitted token reading.
The final consumer source has SHA-256 `84D008BDDED7D5B28927FCD73CD8FE168820378BDE788B65BCDA2560438057E0`.
Its locked dependency graph has SHA-256 `9B13E107125F10C4576C252887307F472F3037C05827382B6A2B4A238698DE69`.

## Qualification and friction

Stable Rust `1.98.0` formatting, locked and offline all-target checking, strict pedantic Clippy with warnings denied, and execution passed.
The actor initially chose one-byte copied state and event values while adapting the public temporal example.
Strict Clippy correctly refused those values at the public reference-taking callback shape, and the scratch consumer adopted the documented example's wider value posture without a suppression or product change.
The harness README routes to the packaged temporal example rather than duplicating its code, and the compiler README routes to its complete packaged callable example.
The actor needed those public examples for exact imports and constructor shape but needed no internal material.
The documented workspace commands require an external consumer to express its own dependency manifest, which is ordinary Cargo ownership rather than hidden Macroonz knowledge.
No missing checked join, false public shape, or convenience API was observed.

## Host, custody, and ceilings

The host was Microsoft Windows 11 Home version `10.0.26200`, 64-bit, targeting `x86_64-pc-windows-msvc`.
Rustc was `1.98.0 (88d9e12ae 2026-08-18)` with LLVM `22.1.8`.
Cargo was `1.98.0 (797e8a9bc 2026-08-05)`.
The exact Cargo target reached 923 files and 640,879,814 bytes.
Cargo removed all 923 files immediately after the successful observation, and the exact target is absent.
The public packaged examples remain the replayable product owners.
The disposable actor source, lockfile, package extraction, and raw command output do not enter Git.
No payload, product source, public API, feature, dependency, package, qualification architecture, push, merge, ref movement, hosted action, registry action, tag, or publication was created.
This accepts only the local Windows current-package docs-blind storefront plane.
