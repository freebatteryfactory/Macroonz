# Macroonz 0.2 Wave M factoring ruling

## Standing

This receipt records the Wave M orchestration and factoring decision at repository revision `18557b5c738cf36d7079561f006ef78827138157`.
The ruling is no new product surface.
It preserves every public value already earned during the 0.2 line and refuses another abstraction merely for visual unity, macro enthusiasm, or evidence-dashboard convenience.

## Already-earned compiler structure

The 0.2 product delta already contains the smallest generic structural additions earned by Wave B: `KeyedRoster`, `KeyedRosterAssignment`, their typed refusals and ordered readers, and `keyed_roster_slice` plus `keyed_assignment_slice`.
The version-evolution and guarded-transition subjects independently adopted those values before the current package journeys.
The guarded subject uses those values for closed vocabularies, exact payload assignments, duplicate and foreign-reference refusals, completeness, retained order, canonical encoding, and ordinary Rust projection while retaining its own grammar, meanings, diagnostics, policies, and renderer choices.
No accepted subject required a public relation ontology, state-machine ontology, declaration-algebra subsystem, generic transition type, or Macroonz-owned domain vocabulary.
The current informed values and token composers therefore remain the paved structural batteries, and no larger algebra object is promoted.

## Existing harness composition

The canonical journeys compose the existing public homes directly.
The concurrency journey joins fault, interleave, properties, runner, reduction, replay, identity, report, and a caller-owned process boundary without a pipeline owner.
The parser journey joins stable coverage, corpus, reduction, replay, vector judgment, mutation pressure, and report custody without a second engine.
The cohesive journey joins architecture observation, fault injection, schedule pressure, impact movement, stable coverage, mutation readback, and previously accepted economics without importing the subject into Macroonz.
The docs-blind package actor completed useful compiler and harness work from package documentation and examples and observed no missing checked join or convenience API.
Those results favor direct typed composition under existing module homes rather than `CampaignBuilder`, one giant campaign enum, a plugin registry, or another configuration schema.

## Repeated glue disposition

The two stable-coverage journeys repeat process and LLVM-tool choreography, but both already enter through `macroonz_harness::fuzz` informed readiness, coverage observation, novelty, and interesting-byte values.
The remaining repeated work is caller-owned target compilation, process supervision, scratch placement, and subject-specific classification, so it does not earn another fuzz owner or an optional host package.
The two mutation readback binaries share backend-adapter shape but differ in source custody, claim mapping, target binding, and subject coordinates.
Their common semantics already live in the public Muterprater adapter, while the remaining source and console arrangement is campaign-specific and would be misleading as an authority-free packaged example.
The cohesive architecture check and Wave L manifest observation both read manifests, but one establishes a subject-declared package graph and the other derives repository evidence navigation.
Physical input similarity does not make either meaning a public Macroonz architecture API.
Package extraction, scratch workspace boundaries, Git blob joins, receipt hashing, source payload construction, and tool invocation are qualification custody rather than adopter behavior.
They remain outside product authority and do not earn a sealer, launcher, general evidence package, or command host.

## Documentation and stamps

The current package READMEs and runnable callable-compiler, temporal-property, compile-contract, and stable-coverage examples already own the public starting roads they establish.
The accepted docs-blind crossing found one actor-authored lint issue and no missing Macroonz documentation or API seam.
No new README layer, procedural stamp, mutation example, architecture declaration format, or subject grammar is added merely to restate the accepted journeys.
Subject-owned macros continue to compose the compiler's existing informed values and token operations while keeping grammar and semantic authority local.

## Derived evidence model

Wave L has one accepted consumer: the current repository campaign.
Its versioned model and two renderings remain a compact external Git-tracked derivation under `.durafx`, not a compiler type, harness home, facade feature, registry package, or CLI schema.
The retained source is sufficient to replay that derivation, and multiple independent product consumers have not earned another owner.

## Rejected candidates

- No new public campaign value or home is earned.
- No public repository-architecture API is earned.
- No optional process-host package is earned.
- No CLI or command host is earned.
- No new procedural macro or reusable stamp is earned.
- No new declaration-algebra or relation type is earned beyond the accepted keyed structural values.
- No private product helper is warranted for qualification-only custody or process choreography.
- No additional dependency, feature, package, workspace, unsafe road, nightly road, or filesystem sealer is warranted.

## Acceptance and ceiling

Wave M is accepted as a no-new-surface factoring decision at this exact Git-tree and local Windows evidence plane.
The ruling does not claim that future independent adopters cannot earn a narrower helper, host, or command surface.
Any future promotion must identify the same values, ordering law, failure-prone join, custody transition, or impossible intermediate state across genuinely independent consumers and must still prefer an existing owner.
Wave N may now close the approved 0.2 package line without carrying an unresolved orchestration or factoring fork.
