# 0.2 Wave B guarded-transition targeted mutation

## Accepted plane

This receipt accepts one local Windows stable-Rust targeted structural-mutation observation over the exact guarded-transition semantic source previously accepted at `3cf8788`.
It establishes that the source's focused witnesses reject every viable mutation selected from the transition, effect, association, policy, projection, and disposition-reading operations under the declared Cargo Mutants selector.
It also establishes that the initially surviving structural damage was repaired in the disposable subject source and promoted into exact external claims before scaling.
It does not close scaling, economics, independent external pressure, cross-host qualification, Wave B, or Macroonz 0.2 acceptance.

## Repository authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Product baseline: `3cf878865045d8612e5391e3feab089e7af89b93`.
- Semantic input: `.durafx/qualification/0.2-b-guarded-transition-20260829/source-payload.tar.zst` at SHA-256 `BB5E445AC0554FB1A550EC515BC1AE11E1DDC93056585FBC37CDBF16A5D125F7`.
- The tracked product worktree and index remained clean until this receipt and payload were added.
- All repairs, tests, mutation output, and custody composition remained beneath disposable `target/qualification` material until sealing.
- This observation earned no product source edit, API, owner, feature, dependency, package, facade edge, identity grammar, mutation engine, or qualification architecture.

## Initial red observation

The first exact campaign selected seventy mutants against the accepted semantic source.
It reported forty-two caught, seven compiler-unviable, twenty-one viable misses, and zero timeouts.
The complete first-run outcomes file has SHA-256 `4A03AC0825480B035AC2D1E7827626872823A5F4EA5D3853D5CBCE40E4A0E376`, and the exact missed roster has SHA-256 `95762C0757C43A016DB4FB90E407ECA46739FE816E0A1CF9C5C878DD87B9DE4F`.

The surviving rows exposed three real evidence weaknesses and one equivalent mutation:

- compound punctuation predicates allowed a broad refusal-family assertion to pass without proving the exact wrong token;
- compound membership predicates returned the right refusal family while pointing at the wrong foreign member;
- the missing-effect assertion did not name the exact omitted transition;
- changing the backward-order comparison from greater-than to greater-than-or-equal was equivalent because equality had already been refused.

Red was treated as evidence rather than silenced.
No mutant was removed from consideration merely because it was inconvenient.

## Promoted repair

The disposable subject engine now reads transition, effect, association, and policy punctuation at separate exact token seats.
Foreign transition, effect, association, and policy members are refused at the exact offending member.
Transition order uses one total `cmp` match, so self and backward states remain distinct without a redundant equivalent comparison.
The missing-effect claim names the exact omitted transition.
External engine claims now challenge every punctuation seat and every foreign transition or effect member through both the typed issue and the exact byte coordinate.

Splitting those checks first pushed `read_effect` past the repository's one-hundred-line ceiling.
The final source extracts only the effect-punctuation reading into its own operation, stays within the line ceiling, and passes strict Clippy without a suppression.
That private refactor was included in the final mutation denominator rather than treated as post-campaign cleanup.

## Final mutation denominator

The final command retained Cargo Mutants `27.0.0`, stable Rust `1.98.0`, the same engine source, the same function-name selector, the same focused external test target, one Cargo job, no shuffle, locked and offline Cargo, explicit Windows x64 target, and 120-second build and test ceilings.
Its exact command tokens are retained in the external mutation-custody claim inside the source payload.

The final source selected forty-five mutants:

- thirty-seven were caught;
- eight were compiler-unviable;
- zero were viable misses;
- zero timed out.

The selected count fell from seventy to forty-five because the repair removed compound boolean operators and one redundant comparison rather than excluding files, functions, or mutation genres.
The final punctuation helper introduced one new return-value mutant, and that row was compiler-unviable.

Seven unviable rows replace `read_transitions`, `read_effects`, `read_effect`, `read_associations`, `read_policies`, `read_projections`, or `accounted` with a default success value whose payload type does not implement `Default`.
The eighth replaces `read_effect_punctuation` with `Ok(())`, which leaves all five named inputs unused and therefore fails compilation under the subject's denied-warning wall.
The exact final outcomes file has SHA-256 `17D2C927F7FC16BA725D02A2C71F1607532665CD85BC1F9C6EEA466CEDA16E74`.
The final caught roster has SHA-256 `4E807D36C32078AB511F7F80D27CE4DA7CBACABADE19252B6EE360579ECB225A`.
The final unviable roster has SHA-256 `683B94EC1AD3F73316497F5C3CE276A15F8923A74909FEB0F830C9635CC99EC2`.

## Existing Muterprater custody

The source payload retains a compact host-neutral Cargo Mutants console at SHA-256 `CA60B0A133BE799B765676A77F79CF5C401A004C3415D4ED31FD90F19ECC0DAB`.
One external subject test reads that console through the existing public `muterprater::wrap::read_artifact` road.
`MutationSourceRevision` binds the reported engine path to the exact final engine bytes at SHA-256 `6E61F5EE29C055FD1876687B19ED3CB9AFFCC6E4BEE1C22CF0C9A7D8AC5F3924`.
`MutationBackendInvocation` retains the backend, version, command tokens, target, and toolchain.
`AdapterQualification` binds the checked console grammar to the exact backend version.
`CompiledSuiteArtifactCustody` joins the complete one-file artifact roster to the complete current-source roster.
`CompiledSuitePressure` admits a lawful backend-reported kill from that current-source-qualified artifact.

All forty-five targets deliberately remain owner-unmapped and outside the operator-family bank.
This campaign establishes backend-reported witness rejection over one exact source revision; it does not manufacture activation, equivalence, semantic mutation ownership, another host, or general mutation adequacy.

## Stable wall and package boundary

The final exact source passed:

- stable 1.98 formatting;
- sixteen focused external compiler-engine claims;
- the twenty-one-conclusion semantic and diagnostic journey;
- the external Muterprater custody claim;
- the complete serial locked and offline workspace suite with all unit, binary, integration, and doctest targets;
- all-target check and build;
- workspace documentation with warnings denied;
- strict all-target Clippy with warnings denied;
- locked and offline metadata plus dependency-tree inspection.

Metadata names exactly the three scratch subject packages as workspace members.
Every Macroonz manifest resolves beneath the extracted package-candidate tree used by the accepted semantic pilot.
All consumers still use only the `macroonz` facade renamed to `bakery`, and only the external challenge enables its `harness` feature.
No live-workspace or registry Macroonz fallback was observed.

The twenty retained source files use LF and contain no unsafe operation, `#[allow]`, `#[expect]`, library `#[cfg(test)]` item, authored build script, personal identity, absolute user path, or reparse point.
The derived expansion reading remains byte-identical to the accepted semantic input at SHA-256 `177818F18609FFE97F2186E9871942D0133C15559C06A67620FE2E40D2A702D4`, so the repairs moved diagnostic exactness and witness strength without moving accepted expansion semantics.

## Exact source custody

`source-payload.tar.zst` retains the root scratch manifests, lockfile, toolchain declaration, complete final authored source, exact final Cargo Mutants console, external Muterprater custody claim, and unchanged derived expansion reading.
It contains twenty-one files and excludes extracted Macroonz packages, compiled targets, prior mutation runs, raw runtime output, absolute paths, and private paths.
Two independent payload creations produced the same 33,945-byte archive with SHA-256 `C7CD880A9DC4C7889E40F9333C616F463C2D2C36CAEFE23EC66BD346E11B60BF`.
Independent parent extraction reproduced all twenty-one retained files byte for byte.
The host-neutral scratch qualification record is 9,996 bytes with SHA-256 `26A4D5002AE0A7866BFD627D5336ECBB53AFE2445BC47549EE17852E6A66EA33`.

## Disposition

Targeted structural mutation is accepted for the exact guarded-transition source retained here.
The initial viable misses were promoted into the owning disposable subject source and its external regression plane, and the final exact denominator has no viable miss or timeout.
Existing Muterprater custody was sufficient, so the mutation carrier still earns no Macroonz product seam.
This final exact semantic source is now the only lawful input to the guarded-transition scaling and planted-worse economics lot.
After this receipt and payload are committed and independently verified, the exact mutation build targets and prior mutation-run output may be cleaned when no active task depends on them.
