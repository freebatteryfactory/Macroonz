# Macroonz 0.2 current structural-battery mutation

## Accepted plane

This Git-tracked receipt accepts one local Windows stable-Rust targeted mutation observation over the adopted guarded-transition engine and the three structural compiler batteries that engine now consumes.
It establishes that the adopted engine has no viable survivor under its exact current-source selector and that every viable public diagnostic mutation exposed by the structural-battery selector is rejected after the missing external witnesses were promoted.
It records one equivalent payload-alignment mutation separately rather than changing product code or manufacturing a witness for a structurally unreachable case.
It does not establish another host, hosted execution, general mutation adequacy, or Macroonz 0.2 acceptance.

## Repository authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Opening product revision: `c3894108b2c63e177165d58736f6f33ffa39c01b`.
- Caller-keyed roster commit: `1c1711fa9d943bcce0d5461aaf12574bc34b9450`.
- Exact keyed-assignment commit: `d78df38d3dd13fc0287361ec66b6468c14f9b6eb`.
- Structural projector commit: `b353f71cd5cded0d29e9c9b83dc2531d1ccc52d5`.
- Guarded-transition adoption commit: `c3894108b2c63e177165d58736f6f33ffa39c01b`.
- Adopted source payload: `.durafx/qualification/0.2-b-guarded-transition-structural-adoption-20260830/source-payload.tar` at SHA-256 `0682911A9A6EB947248764DA6608C8C4ECD9C3B58A818ADA3FC160CACB340AE1`.
- No product algorithm, public API, feature, dependency, package, facade edge, identity grammar, mutation backend, or qualification architecture changed in this cure.
- This lot authorizes no push, merge, ref rewrite, publication, registry mutation, nightly toolchain, or cross-host claim.

## Tools and command contract

The campaign used Cargo Mutants `27.0.0` through Cargo `1.98.0 (797e8a9bc 2026-08-05)` on `x86_64-pc-windows-msvc`.
The exact compiler was `rustc 1.98.0 (88d9e12ae 2026-08-18)`, commit `88d9e12ae178fab0fb5cc050a94da85685d449ea`, with LLVM `22.1.8`.
Cargo work was serial, locked, offline, fixed to one build job, and bounded by 240-second build and test ceilings.
The authoritative build and test phases both received `--locked`, `--offline`, the exact Windows target, and one Cargo job.
The product scenarios selected the existing `bounded_collections`, `structural_token_projection`, and adopted `engine_claims` external targets through the scratch workspace.
Cargo Mutants `27.0.0` baselines only the mutated package, so the product run used `--baseline skip` and surrounded the campaign with independent exact workspace baselines over all three targets.
The pre-cure baseline passed 50 of 50 claims, and the restored post-cure scratch baseline passed 54 of 54 claims.

## Current adopted-engine denominator

The engine selector admitted exactly fifty-seven mutations from the adopted engine at SHA-256 `903AC855D941E2EBC3DF447BE8F55A5DA2AE94581C0592301E3EB327ED3A7D46`.
Its external claim source had SHA-256 `AE1876AD907B3067EE85F32299A7DA97C9FAA495FA4AE7F13546394F4960EAC0`.
The authoritative run accounted for every selected row:

- 57 selected;
- 37 caught;
- 20 compiler-unviable;
- 0 missed;
- 0 timed out.

The exact engine `mutants.json` had SHA-256 `F624B1609FC65EAB093E32BA062B1C314AD2B160A5CBC850DD494AADDB1A2E25`.
The exact engine `outcomes.json` had SHA-256 `C34FFA79588E81B95164324978B17F7A9479D6DA5AC7ADFEBA33B01BFFCD3933`.

## Product-battery falsification

The product selector admitted exactly eighty-six mutations from:

- `macros/compiler/src/bounded/type_guard.rs` at SHA-256 `10A2A6D69E3FD3F30C424E790F4D5DC4A69D78E5D1AEA924DD6A9ED070C9623F`;
- `macros/compiler/src/bounded/type_contract.rs` at SHA-256 `51EFDCFFFACCE02FCF26EF90038433728C7D123B718A715A54478B8ADA974B36`;
- `macros/compiler/src/token/generation/project.rs` at SHA-256 `0B9BD218D718E47C07CEB9D93737556F583E6EBFAC897C082AB30FAB4046E8F8`.

Those files came from the extracted compiler package whose VCS revision was `44db7e06e4f317c52788f357dc134f924ea88446`, and each remained byte-identical to the corresponding tracked implementation.
The authoritative pre-cure run reported:

- 86 selected;
- 24 caught;
- 48 compiler-unviable;
- 14 reported missed;
- 0 timed out.

Thirteen reported survivors changed public singular-versus-plural refusal text:

- the caller-keyed roster duplicate count at line 64;
- assignment foreign-reference magnitude at line 91;
- assignment duplicate-reference magnitude at line 99;
- assignment reused-payload-seat magnitude at line 106;
- assignment missing-member magnitude at line 114.

The exact mutations replaced guards with `true` or `false` and inverted the cardinality comparisons.
They were real external-evidence gaps because the public sentences changed while the owning integration lane had not independently pinned both magnitude forms.

The fourteenth survivor changed the head-alignment comparison at `type_guard.rs` line 741 from `<` to `<=`.
That mutation is equivalent under the informed construction chain: denominator keys are unique, admitted references are unique before alignment, and distinct references into a unique denominator map to distinct denominator positions.
Equality at that comparison is therefore unwritable, so the mutation cannot change an admitted value.

The exact pre-cure product `mutants.json` had SHA-256 `105646C99A5AD3F1E2F3F04544057C9E465E18312D0AE909EA087DC3ED87E828`.
The exact pre-cure product `outcomes.json` had SHA-256 `9F9999246CACB065021FC35139DA63B29D8F66E30AD42527F9FAD87A76B3FECD`.
The exact fourteen-row missed roster had SHA-256 `4E098D9C113849603CF6A31C06EFDBA82FFC9AB8B0951FD155BD0E633C05B340`.

## Promoted external witnesses

The existing `bounded_collections` integration lane now constructs lawful public refusals and pins both singular and plural sentences for every affected class.
No refusal is forged through a private field, and no library test hook was introduced.
The tracked roster test has SHA-256 `AD129FBE4013B9FB3065E85AE4B0C5AAA021FE6B000727B2C7846449AEEA487C`.
The tracked assignment test has SHA-256 `CCB9189F4F256B2B53E4ACF8EEFEF8BD6EBB216C28C9D0492F32370A7DEB53AC`.
The focused live integration target passed 27 of 27 claims.

The post-cure selector admitted the complete seventeen-member display surface rather than naming only formerly missed rows.
Its authoritative result was:

- 17 selected;
- 15 caught;
- 2 compiler-unviable;
- 0 missed;
- 0 timed out.

The two whole-function replacements were compiler-unviable because their proposed default result could not satisfy the existing type and lint wall.
The exact post-cure `mutants.json` had SHA-256 `EC85D58CCC6691CDDDCE38E08B6D0AF397427F377E616D19F5001141A504DA7C`.
The exact post-cure `outcomes.json` had SHA-256 `4B59B2DD28045843C74877E7F82162558B3A51F226CC9890DFAE0547F1D1AC27`.
Its empty missed and timeout rosters each had SHA-256 `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`.

## Stable wall

- `cargo +1.98.0 fmt --all -- --check` passed.
- The focused compiler `bounded_collections` target passed 27 of 27 tests.
- The package-only post-cure baseline passed the exact 20 engine, 27 bounded-collection, and 7 structural-projection claims.
- `cargo +1.98.0 test --workspace --all-features --all-targets --locked --offline --target x86_64-pc-windows-msvc --jobs 1 -- --test-threads=1` passed with only the repository's declared ignored child and long-campaign seats.
- `cargo +1.98.0 clippy --workspace --all-targets --all-features --locked --offline --target x86_64-pc-windows-msvc --jobs 1 -- -D warnings` passed.
- Workspace documentation passed with all features, no dependencies, locked and offline Cargo, the explicit Windows target, and warnings denied.
- Workspace doctests passed 4 of 4 compiler examples with no failure in any package.
- `git diff --check` passed.

## Custody and ceilings

The raw Cargo Mutants JSON records absolute host executable paths, so it remains disposable under `target/qualification` and is not copied into Git.
Git retains the exact product sources, the promoted external regressions, the already accepted guarded-transition source payload, and this compact receipt with hashes of every authoritative raw result.
Earlier zero-mutant or command-contract experiments and the complete runs whose build phase lacked explicit locked and offline arguments remain excluded operator-shape evidence only.
The authoritative raw homes are `target/qualification/mwbm-e3`, `target/qualification/mwbp-o6`, and `target/qualification/mwbp-cure-o1` until this receipt is committed and independently verified.
This accepts the local Windows current-source structural mutation plane only.
