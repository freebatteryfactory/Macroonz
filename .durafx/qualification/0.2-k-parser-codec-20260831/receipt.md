# Macroonz 0.2 parser and codec public-package journey

## Accepted observation

This receipt accepts one local Windows stable-Rust journey through the current packaged Macroonz facade, compiler, proc-macro, and harness surfaces.
One disposable safe-Rust subject composes subject-owned grammar, generated and handwritten codec parity, stable-rustc source coverage, deterministic novelty retention, typed failure fingerprinting, existing reduction, exact replay, vector judgment, mutation pressure, and Muterprater readback without adding a Macroonz product owner.
The journey closes the selected local parser, codec, coverage-feedback, reduction, replay, vector-oracle, and mutation-sensitivity portions of canonical journey 16.1.
It does not accept all of Wave K, another host, registry-delivered 0.2 artifacts, publication, or Macroonz 0.2.

The pre-seal repository revision was `bfbd079840ed546a14c6b8b79f821d35e269b92e`.
The journey changes no Macroonz product source.

## Package custody

The subject uses all four `0.1.0` package candidates produced at product source revision `485a4751d397d6dbc6fad7990cda712f8a8bbbd0`.
The `macroonz` archive is 129,487 bytes with SHA-256 `31B3A766F65FAD465B7720FEACBF8948759B4510D4597C94B7FBAD2F701846A1`.
The `macroonz-compiler` archive is 408,173 bytes with SHA-256 `710ABD09270473F1131FFEC96456BEA32B8088D1B1D7D4CBCBC3EB8D772824E8`.
The `macroonz-harness` archive is 461,573 bytes with SHA-256 `FF579811BA50E0FBEE8F92C8129E71963D6BD3927C7FC1AB74B43280370D5459`.
The `macroonz-macros` archive is 20,822 bytes with SHA-256 `B07C03C0C7F1FE7EB86B37865C3B183507065457610771237157AD4AD1B16F71`.
Each extracted package carries `.cargo_vcs_info.json` for that exact product source revision.
The subject names only the `macroonz` facade as its direct dependency and renames it `bakery`.
The copied normalized package manifests receive only an empty `[workspace]` table so Cargo cannot attach them to the containing Macroonz checkout.
Locked and offline Cargo metadata resolves exactly four Macroonz packages and every one resolves beneath the subject's `packages/` extraction rather than the live checkout or registry.
The package archives, extracted package sources, and compiled package graph remain disposable and are not duplicated in `.durafx`.

## Subject grammar and codec

The subject owns its declaration grammar, generated records, handwritten records, controlled parser damage, codec meanings, exact vectors, and failure cause.
The proc engine and instrumented parser probe include the same private subject parser implementation rather than maintaining parallel grammar models.
The lawful parser requires `=>` and places a `==` refusal at its second operator token.
`ParserDamage::AcceptSecondEquals` admits `==` only through the explicitly damaged road.
Generated and handwritten records round-trip the same exact 48-byte vector, the public vector oracle accepts that vector, and the oracle refuses one trailing-byte damage.
Malformed generated decoding remains a normal typed refusal.
Five external codec tests passed.

## Coverage, feedback, reduction, and replay

The instrumented final parser probe was compiled by stable rustc with `-C instrument-coverage -C opt-level=0`.
The journey declared the absolute stable-rustc path before building its supervisor and used the matching `llvm-profdata` and `llvm-cov` tools from that toolchain.
Four cases were attempted: one valid seed, one lawful baseline refusal, one controlled defect input, and an exact repeat of the controlled defect input.
The valid seed added 83 normalized source-coverage points, the lawful refusal added 17, and the controlled defect road added 2.
The three novel cases retained 102 total points and 141 total input bytes.
The repeated defect input was classified `Known` and did not enter the corpus again.
Two complete executions produced the same summary with SHA-256 `86705E478CDD5B1B55671F0D6C81100FA5947A9604FA78829AE0E753CE0ED602`.
The controlled counterexample began at 47 bytes and the existing chunk-removal-and-zeroing reducer reached a 32-byte exact replay.
The minimized bytes are hexadecimal `440A7265636F726420447B743A753D3D636F756E742072657175697265642C7D`, which is `D` followed by LF and `record D{t:u==count required,}`.
The replay capsule carries `ExactDerived` posture and reproduces the same typed `PropertyDisagreement` under cause `shape-subject/parser-accepted-double-equals`.
That exact minimized counterexample is promoted into the external proc-macro regression plane.

## Mutation sensitivity

Cargo-mutants `27.0.0` pressed one exact subject-parser mutation after the minimized regression was promoted.
The neutral command was:

```text
cargo mutants --dir . --in-place --no-config -p shape-subject-macros -f shape-subject-macros/src/parser.rs -F ^shape-subject-macros/src/parser\.rs:138:24: replace == with != in read_members$ --shard 1/2 --sharding slice --baseline run --no-times --colors never --caught -o observations -C --target-dir=build-target -C --jobs=1
```

The selected coordinate was `shape-subject-macros/src/parser.rs:138:24: replace == with != in read_members`.
The unmutated baseline passed and the one selected mutant was caught, with zero misses, timeouts, or unviable results.
The parser source SHA-256 was `46BF95A31F67F044392AA798114D55BB851E2C6BA28CC42787A6E8278FFFBADD` before and after the in-place run.
The public Muterprater adapter read the actual console artifact, retained one pressed and one killed report, recorded zero survived or inconclusive reports, and bound the report to the exact compiled-suite source revision.
The console backend cannot expose per-test activation, so the typed activation posture is `UnobservableUnderBackend` rather than an inferred claim.
One roster line remained explicitly unparsed.
Cargo-mutants supplied mutation pressure only and was not treated as a second fuzz engine.

## Source custody

The external subject cannot enter Macroonz product authority, while its exact source is necessary to replay this cross-owner composition.
The adjacent `source.tar.zst` is therefore the minimal warranted `.durafx` payload rather than a campaign laboratory.
It contains exactly fifteen source, manifest, lock, toolchain, test, and subject README files.
The payload is 18,021 bytes with SHA-256 `A9BDFA6695B8FCB77D80DA4307E16EDE7683B8C8725715C68F131F41394543D9`.
Two independently created USTAR-plus-Zstandard archives were byte-identical, extraction reproduced every member hash, and the archive contains no reparse point.
The member hashes are:

- `Cargo.lock`, 4,386 bytes, SHA-256 `DECEAD0D27966B6717160BBCE78E12A6E7B868DA77F2A115A1996E28598BF0A2`.
- `Cargo.toml`, 1,050 bytes, SHA-256 `386A5D53A50D90F9BBEC80DC5385B39047CB533D89ECFCF3FA51B3A9E2738274`.
- `README.md`, 1,688 bytes, SHA-256 `51B0C7416891F2637734377E5C222B346F266E51D560CD6F833B392CA65687FC`.
- `rust-toolchain.toml`, 87 bytes, SHA-256 `71342820693D6EC2DB785CD0EC39102D6F10F6A59DC29DFEA0E7565123E9CCEF`.
- `shape-subject-macros/Cargo.toml`, 295 bytes, SHA-256 `1BB352DC3E75C170DBAE6C8E028BB1245CE598133F026D9C2525E5836C80E216`.
- `shape-subject-macros/src/engine.rs`, 8,843 bytes, SHA-256 `CD3ADCD044133892BBADAF897AE7559E05272866A0EE57D899B1D763C8C62591`.
- `shape-subject-macros/src/lib.rs`, 333 bytes, SHA-256 `6AF1CC9B6360C5089D3831C131BACED466CA3EA3A74E4FFB00CD85E2062FE21B`.
- `shape-subject-macros/src/parser_probe.rs`, 1,193 bytes, SHA-256 `511A87318C2E4A3D9BA824D5E4D585E7EA5D3512A59A0DA06F588D290E2E9AD3`.
- `shape-subject-macros/src/parser.rs`, 6,095 bytes, SHA-256 `46BF95A31F67F044392AA798114D55BB851E2C6BA28CC42787A6E8278FFFBADD`.
- `shape-subject-macros/tests/engine_claims.rs`, 7,710 bytes, SHA-256 `7F3793AA459B41745D63E9ED609BDFEC755807FA6ABB3B94A7A972C89A994B3B`.
- `shape-subject/Cargo.toml`, 287 bytes, SHA-256 `045C008426A88B040947D26F8E623475D6B1F6CB01C66C6CDB1F6F0A1B203D02`.
- `shape-subject/src/bin/mutation_readback.rs`, 6,072 bytes, SHA-256 `1A3F1B1C06DFFAF4FC6F213CB330EABAB9A611EE63DBECF791D9CC7280012BFF`.
- `shape-subject/src/bin/parser_journey.rs`, 13,187 bytes, SHA-256 `CC4045E413495E0FFC4FA8C9F6651F275A2204D795ACC659B0E4168388E31163`.
- `shape-subject/src/lib.rs`, 6,489 bytes, SHA-256 `8BC4DAED2EC5B9BECE7CB3A3BC82B26E32FF62B84102E06F314B66455512D128`.
- `shape-subject/tests/codec_behavior.rs`, 9,384 bytes, SHA-256 `0FCE8AE7633A1FCED57EDC18A0F7FCE97C3692C7A78CFEC8FA7D1D7830E1D3BA`.

Replay materializes the four exact package archives beneath `packages/`, adds the empty scratch-only `[workspace]` boundary to each copied normalized package manifest, and runs the retained subject with the locked graph.
Package archives, package extractions, raw profiles, coverage-tool output, mutation logs, observations, duplicate archive builds, and Cargo output do not enter Git.

## Qualification and ceilings

Rustc was `1.98.0 (88d9e12ae 2026-08-18)` with LLVM `22.1.8` on `x86_64-pc-windows-msvc`.
Cargo was `1.98.0 (797e8a9bc 2026-08-05)`.
The matching LLVM coverage tools reported `22.1.8-rust-1.98.0-stable`.
The host was Windows 11 Home version `10.0.26200` on x64.
Stable formatting, locked and offline four-package metadata, locked and offline all-target checking, strict all-target Clippy, all-target tests, two coverage executions, exact replay, exact vector judgment, and one selected mutation all passed.
Seven external proc-macro tests and five external codec tests passed.
The one exact Cargo target contained 3,958 files and 1,531,778,646 bytes before cleanup.
Cargo removed all 3,958 files, reported 1.4 GiB removed, and the exact target is absent.
Three superseded raw profile files totaling 2,084,304 bytes remain outside accepted custody because host policy refused their exact nonrecursive removal.
Those raw profiles are excluded from the payload and confer no evidence authority.
The journey establishes deterministic stable-rustc source-region feedback for this declared parser subject, not edge coverage or nightly branch coverage.
No second fuzz engine participated, so no method comparison is claimed.
The observation does not establish general parser correctness, arbitrary grammar support, another codec, another architecture, another operating system, a hosted runner, registry-delivered 0.2 packages, or publication.
