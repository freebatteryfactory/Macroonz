# 0.2 Wave B version and evolution structural adoption

## Accepted plane

This receipt accepts one local Windows stable-Rust adoption crossing over the already accepted version and evolution subject.
It establishes that the new caller-keyed roster, exact keyed assignment, and paved slice projectors can replace repeated subject compiler plumbing without changing the accepted grammar, diagnostics, generated bytes, behavior, ownership, or package graph.
It does not establish guarded-transition adoption, targeted mutation over the new structural owners, another host, Wave B acceptance, or Macroonz 0.2 acceptance.

## Repository and package authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Product revision: `b353f71cd5cded0d29e9c9b83dc2531d1ccc52d5`.
- The tracked product worktree and index remained clean throughout the scratch adoption and parent review.
- Fresh `.crate` candidates for all four product packages were created from that exact revision and extracted beneath disposable scratch.
- Locked and offline metadata resolved every Macroonz package manifest beneath the scratch package tree, with no registry or live-worktree Macroonz fallback.
- Every extracted package carried `.cargo_vcs_info.json` revision `b353f71cd5cded0d29e9c9b83dc2531d1ccc52d5`.
- The facade remained renamed to `bakery`, and only the external challenge enabled the facade harness feature.
- No public API, dependency, feature, package, facade edge, semantic owner, identity grammar, or qualification architecture was introduced by this adoption.

The package archive SHA-256 values were:

- `macroonz-0.1.0.crate`: `135427EC33898FEE1DE4D3D70D658E22D41F81BEF33383EC0D0531563B6A4E84`.
- `macroonz-compiler-0.1.0.crate`: `0933566858768C65EF269C82487EF6C8A15AABDE095BFBE9FA130E12E1A31CAE`.
- `macroonz-harness-0.1.0.crate`: `DAFF25F434AADC3684A99A8FA596EB8BF264BF55FF10F9B106A0DE64AD39F227`.
- `macroonz-macros-0.1.0.crate`: `D3E71BD79222AF7915D9A3B2B5E6DD7A06CE28102B8DAB12ED6E24EBCCAA859D`.

## Adoption and authority split

The accepted subject previously stored versions, edges, and adapters in parallel bounded lists and reconstructed their alignment in subject code.
The adopted subject stores versions in `KeyedRoster` and stores the exact edge-to-adapter account in `KeyedRosterAssignment`.
Version lookup now uses `KeyedRoster::index_of`, and lawful adapter traversal now follows the informed denominator order without a second rendering-time sort.
The version and edge constants now use `keyed_roster_slice` and `keyed_assignment_slice`, while the subject still owns their surrounding constants, types, names, paths, visibility, and semantic contents.
The unchanged raw renderer road consumes the same informed values through their public readers.

The subject retains its grammar, vocabulary, forward-only edge law, adapter effects, canonical-content selection, request, plan, role, closure, diagnostics, and alternate-renderer behavior.
Small early duplicate scans remain only to preserve the already accepted subject fail-fast diagnostic class and exact token placement.
Those scans do not mint lawful structure; the keyed constructors are the only lawful-value boundary.
The authored edge order remains transient only to preserve which missing adapter the legacy diagnostic names, while stored lawful edge and adapter order remains canonical.
An attempted prefix-by-prefix reconstruction through the public roster constructor was rejected during parent review because it duplicated work and would have turned the adoption into a quadratic workaround.
No incremental roster-builder API was promoted because this one subject diagnostic posture does not independently earn another product seam.

## Exact compatibility evidence

Only three of the accepted eighteen retained source files changed:

- Root scratch `Cargo.toml`: SHA-256 `7186F975D1D3DF94ADD1CE2ABD0E7F10565949D6FE137714C56F583BAC52A651`, with current scratch package paths only.
- `evolution-subject-macros/src/engine.rs`: SHA-256 `9DCFB22EDDBFD9783DC3DFAF182309BF59A9116814BC083A8E8100527EC88575`.
- `evolution-subject-macros/tests/engine_claims.rs`: SHA-256 `AAA7F0EEA76703B9FF81B13C39C3BC2E9FD625BFE29D8EDE7B65513E227ADC2E`.

The other fifteen retained files, including `Cargo.lock`, remained byte-identical to the accepted version and evolution payload.
The compiler-side external lane passed fifteen claims.
Those claims preserve every earlier lawful, hostile, movement, nonmovement, alternate-renderer, and closure observation and add exact mixed-hostile diagnostic class and token-span precedence.
They also prove duplicate precedence after an overbound prefix, authored-order first-missing-adapter selection, and independently written raw-versus-paved canonical-byte parity for both the baseline and direct-edge declaration.
The external effectful journey passed its complete report crossing.

Generated, alternate-generated, and handwritten readbacks remained byte-identical at SHA-256 `FC4C1E900B14CE60F204125EE8541495955202355BA58752694AA06F3E23A987`.
Each reports value `19`, versions `V1`, `V2`, and `V3`, and the same two canonical edges.
The planted direct-edge readback remained byte-identical at SHA-256 `5E00DE96E4CCA4EA85AA0A90E7EF4CC4C81D3F4CF6458531BBCFAE7A641824EC`.
It reports direct and composed value `19` and the canonical three-edge roster.

## Parent wall

The independent parent review read the three changed retained files whole and compared their complete diffs against the accepted source.
The parent reran the fifteen compiler-side claims, the external journey, formatting, the complete serial workspace suite, strict all-target Clippy, workspace documentation with warnings denied, package metadata, package custody, source-law scans, and the four exact runtime readbacks.
All successful Cargo work used stable Rust `1.98.0`, Cargo `1.98.0`, locked and offline resolution, target `x86_64-pc-windows-msvc`, and serial execution.
The host was Microsoft Windows 11 Home version `10.0.26200`, build `26200`, 64-bit.
Rustc was `1.98.0 (88d9e12ae 2026-08-18)` with LLVM `22.1.8`.

Two parent commands were excluded as operator-shape errors before correction.
`cargo rustdoc --workspace` was rejected because that Cargo subcommand does not accept `--workspace`; the intended warnings-denied workspace documentation wall passed through `cargo doc --workspace --no-deps`.
Four initial readback commands used hyphenated implicit binary names and were rejected by Cargo; the declared underscore target names then ran and reproduced the exact accepted outputs.
Neither command error reached a product observation or altered source.

The retained eighteen-file source contains no unsafe operation, `allow`, `expect`, library `cfg(test)`, personal identity, absolute host path, build script, CRLF text, or reparse point.

## Exact source custody

`source-payload.tar` retains exactly the root scratch manifests, lockfile, toolchain declaration, and complete authored source and external tests of the three subject packages.
It contains no extracted Macroonz package, package archive, compiled target, runtime output, Cargo JSON, absolute host path, or general build output.
Two independent payload creations produced the same 133,632-byte archive with SHA-256 `C60ED2403057C6701C725333DD742ED83FD2A27BC4A1EE92BA3DDEFE6E9516A6`.
Independent extraction reproduced all eighteen retained files byte for byte.

## Disposition

The version and evolution subject adopts the three accepted structural batteries without semantic or compatibility movement and without another product mechanism.
This acceptance is limited to the exact local Windows source, package set, target, and stable toolchain named here.
The next adopter is the accepted mutation-final guarded-transition source, not the stale preserved semantic-pilot scratch.
Guarded-transition adoption must begin from `.durafx/qualification/0.2-b-guarded-transition-mutation-20260829/source-payload.tar.zst`, whose accepted engine SHA-256 begins `6E61F5EE`, and must repeat package-only authority, diagnostic, hostile, generated-versus-handwritten, and stable-wall crossings before any new structural seam is proposed.
