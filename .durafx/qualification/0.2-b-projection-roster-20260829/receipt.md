# 0.2 Wave B subject-owned projection-roster pilot

## Accepted plane

This receipt accepts one local Windows stable-Rust subject-repository observation against the current Macroonz 0.2 campaign package surface.
It establishes that one subject-owned proc macro can compose the existing compiler projection, accounting, facade, compiled-oracle, runner, and report batteries without a Macroonz product change.
It does not close Wave B, the complete declaration-algebra denominator, cross-host qualification, mutation pressure, scaling cost, or Macroonz 0.2 acceptance.

## Repository authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Product baseline: `59976f0fc5ac26b45562f10d712f086ddd450b31`.
- The tracked product worktree and index remained clean throughout the subject run and independent parent review.
- The disposable subject resolved no Macroonz package from the live workspace.
- This observation earned no product source edit, public item, feature, dependency, package, first-party proc entry, identity grammar, structural-oracle extension, harness lane, or qualification architecture.

## Subject topology

The disposable workspace contains exactly three subject packages:

- `surface-subject-macros`, which exists because Rust requires a proc-macro package boundary and owns the subject compiler engine;
- `surface-subject`, which owns the public trait, macro declaration, generated road, and direct handwritten equivalent;
- `surface-challenges`, which exists only to observe the two roads from a genuinely external package boundary and owns the compiled-oracle, runner, and report claims.

The proc library retains only compiler-host capture, one private subject compiler call, typed diagnostic placement, and emission of the sealed expansion.
The private subject engine owns the narrow declaration grammar, canonical projection content, public role, generated Rust rendering, kind denominator, and post-expansion disposition decisions.
The macro invocation explicitly declares the contract path and member, generated type, member type and value, public role, and declaration-site destination.
The grammar carries no selector, damage mode, disposition, or accounting clause.
The generated projection emits an ordinary public marker and subject-trait implementation.
The direct road handwrites the same public contract without reading compiler internals.

## Existing batteries composed

The subject uses the existing compiler host capture, span, diagnostic, canonical-content, request, plan, closure, explanation, expansion, generated-token, and emission surfaces.
The subject declares its complete projection denominator with current `kinds!` before rendering.
The generated unit identity is selected from sealed plan membership by typed `PublicSurfaceRole::PublicApi` rather than by position.
Generated, not-requested, and not-applicable dispositions are completed only after expansion and seated with `Accounted`.
Projection dispositions never enter `Kind::Content`, request input, or the existing identity chain.
The external challenge uses the existing compiled oracle, runner admission, report standing, failure classification, revision binding, and replay-posture surfaces.
Compiler values do not cross into harness authority, and the harness owns neither the subject grammar nor the expected generated value.

## Independent behavior and non-vacuity

The compiler-side external target passed five tests.
Repeated identical declarations preserved captured input, canonical content, plan, closure, explanation, and expansion readings.
Changing the declared member value from `73` to `74` moved every one of those semantic readings.
Changing only the consumer disposition preserved exact expansion equality and changed the complete disposition set and `Accounted` value.
Omitting the sole planned output produced `MemberMissing` for the public role.
Doubling that output produced `MemberDuplicated` with observed count two.
An incomplete disposition record produced `CountMismatch`.
A foreign disposition substitution produced `KindMismatch` at the exact expected and observed kind names.

The effectful external challenge passed one journey containing six complete runner rows.
The generated executable actually ran and read back `VALUE=73`.
The independently handwritten executable actually ran and read back `VALUE=73`.
The compiled oracle held both roads to one declared read-back roster.
An explicitly wrong declared value produced `compiled-member-value`.
Removing the macro road produced stable rustc anchor `E0425` at `surface-challenges/src/bin/macro_removed.rs:7:10-7:26`.
The exact diagnostic contract conformed.
A wrong error-code declaration produced `compiled-diagnostic-error-code`.
A wrong primary-span declaration produced `compiled-diagnostic-primary-span`.
Every conclusion passed through `record_one`, and the complete six-row roster passed through `record_all`.
Every admitted report carried derived subject and check revisions, the exact target and toolchain, and `ReplayPosture::ExactDerived`.

## Package-only crossing

Fresh `.crate` candidates for all four Macroonz packages were extracted under disposable scratch and selected through scratch-local patch rows.
Every candidate cites baseline commit `59976f0fc5ac26b45562f10d712f086ddd450b31` in `.cargo_vcs_info.json`.
Locked and offline metadata reported exactly the three subject packages as workspace members.
It reported every Macroonz manifest beneath the scratch package tree and zero live-workspace fallback.
The proc subject and external challenge reach Macroonz only through the `macroonz` facade renamed to `bakery`.
The ordinary subject reaches only its required sibling proc package directly.

The package archives were:

- `macroonz-0.1.0.crate`, SHA-256 `A367FC184ED945DC7E2E984FA4D782027D1D6386AB07DC6079C4761E69F97EEF`;
- `macroonz-compiler-0.1.0.crate`, SHA-256 `DD991E69DE57DEE53E6EE599C8C3657AD0030693C1751425F442BA509760DFEB`;
- `macroonz-harness-0.1.0.crate`, SHA-256 `BEE6A8711FA7544EAB59925DED61A7D30725260CECE1AE7968B2D166F148BA9C`;
- `macroonz-macros-0.1.0.crate`, SHA-256 `7E35C4558E6933F986EA889BD8D6867C3BDA1989505715361558AF72301A9C32`.

## Stable host and command wall

- Operating system: Microsoft Windows NT 10.0.26200.0.
- Architecture: AMD64.
- Rust target: `x86_64-pc-windows-msvc`.
- Rust: `rustc 1.98.0 (88d9e12ae 2026-08-18)`.
- Cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`.
- LLVM: 22.1.8.
- Cargo execution: serial, locked, and offline after the declared package-preparation step.
- Each effectful child has an enforced 60-second Windows process-tree ceiling.
- Wall measurement is explicitly unavailable rather than invented.

The independently rerun command denominator was:

- `cargo +1.98.0 fmt --manifest-path $scratch/Cargo.toml --all -- --check`;
- `CARGO_BUILD_JOBS=1 cargo +1.98.0 test --manifest-path $scratch/Cargo.toml --workspace --all-targets --locked --offline --target x86_64-pc-windows-msvc --target-dir $scratch/build-target -- --test-threads=1`;
- `CARGO_BUILD_JOBS=1 cargo +1.98.0 build --manifest-path $scratch/Cargo.toml --workspace --all-targets --locked --offline --target x86_64-pc-windows-msvc --target-dir $scratch/build-target`;
- `CARGO_BUILD_JOBS=1 cargo +1.98.0 clippy --manifest-path $scratch/Cargo.toml --workspace --all-targets --locked --offline --target x86_64-pc-windows-msvc --target-dir $scratch/build-target -- -D warnings -D clippy::all -D clippy::pedantic`;
- locked and offline dependency-tree and metadata inspection;
- source-law, package-candidate, runtime-output, and authored-file hash inspection.

Formatting, the workspace build, and strict all-target Clippy passed.
The test wall passed five external proc-engine claims and one effectful external journey with zero failures and zero ignored tests.
The three library unit-test binaries contained zero tests.
The authored source contained no library `#[cfg(test)]`, `#[allow]`, `#[expect]`, unsafe operation, personal identity, or absolute user path.

## Parent QA corrections

The first effectful challenge draft relied on an interactive shell path to find Cargo.
Parent review refused that host assumption because the independent non-login shell does not expose Cargo on `PATH`.
The accepted challenge uses the exact compile-time Cargo executable and its sibling Rust compiler, then independently verifies their 1.98.0 and host facts before judgment.
The first read-back binaries also used standard output macros that the scratch lint wall forbids.
The accepted binaries use ordinary `std::io::Write`, and strict all-target Clippy verifies the correction.

Parent review also checked the meaning of the row origin rather than changing it by appearance.
`Origin::HandWritten` truthfully describes these six manually authored report rows; it does not claim that every subject they observe was handwritten.
No generated-row provenance was fabricated for the generated subject road.

## Exact source custody

`source-payload.tar.zst` retains only the authored subject source, manifests, lockfile, toolchain declaration, scratch marker, and host-neutral qualification record.
It contains no compiled target, extracted Macroonz package, package archive, registry cache, raw Cargo JSON, absolute user path, or general build output.
The raw compiler output remains disposable because Cargo embeds host-private absolute paths throughout it.
The archive uses an explicit ordered file roster, ustar headers, zstd compression, and a fixed `1970-01-01 00:00:00` modification time.
Two independent archive creations produced the same 16,613-byte payload with SHA-256 `2CA74816D91C788180DF6B87F1F05B970440365661BD3CBF1B3984784F277457`.
The host-neutral qualification record inside the payload has SHA-256 `84B7DF7B37C6C75AD92595E322551EE2D96846B6AAEFF5F98EC053DC20314C98`.
An independent extraction reproduced every one of the 17 retained file hashes.

The payload roster is:

- `.qualification-root`;
- `Cargo.lock`;
- `Cargo.toml`;
- `evidence/qualification-run-v1.txt`;
- `rust-toolchain.toml`;
- `surface-challenges/Cargo.toml`;
- `surface-challenges/src/bin/generated_readback.rs`;
- `surface-challenges/src/bin/handwritten_readback.rs`;
- `surface-challenges/src/bin/macro_removed.rs`;
- `surface-challenges/src/lib.rs`;
- `surface-challenges/tests/journey.rs`;
- `surface-subject-macros/Cargo.toml`;
- `surface-subject-macros/src/engine.rs`;
- `surface-subject-macros/src/lib.rs`;
- `surface-subject-macros/tests/engine_claims.rs`;
- `surface-subject/Cargo.toml`;
- `surface-subject/src/lib.rs`.

## Disposition

The projection-roster candidate reaches reuse-ladder disposition 1: existing composition is complete for this bounded subject behavior.
Current `kinds!`, `DispositionSet`, `Accounted`, request, plan, closure, explanation, expansion, compiled-oracle, runner, report, host, and facade owners already supply the observed mechanics.
The structural oracle remains unchanged because this campaign does not independently earn an expected top-level trait-and-type roster.
No generic declaration frontend, universal relation, expansion-receipt product type, public-surface owner, or new framework seam is earned by this one synthetic subject.
This pilot accepts only the local Windows projection-roster family crossing.
Wave B still owes the remaining structurally distinct families, cross-subject pressure, multi-family composition, cost and scaling measurement, targeted mutation, and the later host planes named by the program.

After this receipt and payload are committed and verified, the exact disposable `target/qualification/0.2-wave-b-projection-roster` tree may be cleaned.
