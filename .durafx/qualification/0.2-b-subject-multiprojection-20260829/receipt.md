# 0.2 Wave B subject-owned multi-projection pilot

## Accepted plane

This receipt accepts one local Windows stable-Rust subject-repository observation against the published Macroonz 0.1 package surface.
It establishes that one subject-owned proc macro can compose the existing compiler, support-carrier, facade, and harness batteries without a Macroonz product change.
It does not close Wave B, the complete transition-system journey, cross-host qualification, mutation pressure, scaling cost, or Macroonz 0.2 acceptance.

## Repository authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Product baseline: `2b1150b0f76ec9ecacaaebc3c57e9b0d60c4d659`.
- Product worktree and index remained clean throughout the subject run.
- The disposable subject used no live-workspace package source.
- This observation earned no product source edit, public item, feature, dependency, package, first-party proc entry, relation home, expansion-receipt home, or qualification architecture.

## Subject topology

The disposable workspace contains exactly two subject packages:

- `transition-subject`, which owns the generated and handwritten behavior and the external judgment lanes;
- `transition-subject-macros`, which exists because Rust requires a proc-macro package boundary.

The subject proc host captures compiler tokens, calls its private compiler road once, places typed diagnostics, and emits the returned proved cargos in order.
The private subject engine owns the grammar, canonical content, kind, projection roles, question, answer, rendering, carrier composition, and subject-specific transition meaning.
The generated production projection defines ordinary `State`, `Event`, and `step` Rust at the declaration site.
The generated checks projection remains inert as proved `TestCarrier` cargo until an external test invokes its subject-chosen carrier address.
The external subject tests route harness judgment through a renamed `macroonz` facade dependency called `bakery`.
The handwritten implementation and generated implementation cross the same independently authored temporal claim at their shared callable boundary.

## Existing batteries composed

The subject uses the existing compiler host capture, span, diagnostic placement, and emission surfaces.
The subject uses the existing `Kind`, `Role`, `Question`, `Answer`, `Request`, `Plan`, `Closure`, `View`, and `Expansion` surfaces.
The subject uses the existing `ProvedCargo`, `SupportAxes`, `SupportAssembly`, `SupportCarrier`, and delivered-carrier surfaces.
The subject-local `Emittable` bundle yields the terminal declaration-site cargo followed by the derived carrier declaration-site cargo.
The external consumer invokes the carrier and uses the existing harness properties and report vocabulary to judge behavior.
No compiler type crosses into harness authority, and the harness owns no subject grammar or generated expected verdict.

## Package-only crossing

The run created fresh `.crate` candidates for all four Macroonz 0.1 packages and extracted them under disposable scratch.
The subject workspace resolved every Macroonz package from those extracted candidates through scratch-local patch rows.
Locked and offline metadata reported only `transition-subject` and `transition-subject-macros` as workspace members.
It reported the four Macroonz package manifests only beneath the scratch package tree, with no live-workspace fallback.
The consumer renamed the facade to `bakery` and enabled its `harness` feature.

The package archives were:

- `macroonz-0.1.0.crate`, 128356 bytes, SHA-256 `EFD8A4FBF2687C3DD096AD2F2658ED2DA353EC66E26789B7C1DE5929F4C754B7`;
- `macroonz-compiler-0.1.0.crate`, 393902 bytes, SHA-256 `4050FA62F06E4C5EEFC731A8986DCBBCCBC1DC652ACCBE9AB3990B1B5685F281`;
- `macroonz-harness-0.1.0.crate`, 460811 bytes, SHA-256 `96DA51C56330ED4C6C9C2D54E84E6A50D1A7FD7005381B8843DB740475F18DC7`;
- `macroonz-macros-0.1.0.crate`, 20823 bytes, SHA-256 `3A0199DF5AB0FB629FE041A2FD6385BFBDCA5ED2F4936920503CE3AF3D753867`.

## Stable host and commands

- Operating system: Microsoft Windows NT 10.0.26200.0.
- Architecture: AMD64.
- Rust target: `x86_64-pc-windows-msvc`.
- Rust: `rustc 1.98.0 (88d9e12ae 2026-08-18)`.
- Cargo: `cargo 1.98.0 (797e8a9bc 2026-08-05)`.
- LLVM: 22.1.8.
- Cargo target: the exact disposable subject scratch `build/` directory.
- Cargo execution: serial, locked, and offline after the declared package-preparation step.

The qualified command denominator was:

- `cargo +1.98.0 fmt --all --manifest-path $scratch/Cargo.toml -- --check`;
- `cargo +1.98.0 test --locked --offline --workspace --manifest-path $scratch/Cargo.toml --jobs 1 -- --test-threads=1`;
- `cargo +1.98.0 clippy --locked --offline --workspace --all-targets --manifest-path $scratch/Cargo.toml --jobs 1 -- -D warnings`;
- `cargo +1.98.0 build --locked --offline --workspace --manifest-path $scratch/Cargo.toml --jobs 1`;
- three locked and offline `cargo check` invocations for the malformed grammar, removed carrier, and removed macro controls;
- locked and offline Cargo metadata and dependency-tree inspection.

Formatting, the workspace build, and strict all-target Clippy passed.
The full test run passed nine substantive tests with zero failures and zero ignored tests.
Two tests were external subject behavior judgments and seven were external subject-engine claim tests.
Both library test binaries contained zero tests, and the library source contained no `#[cfg(test)]`, `#[test]`, or test module.

## Independent behavior and non-vacuity

The lawful carrier exposed exactly one generated transition from `Ready` through `Begin` to `Done`.
The independently authored temporal judge passed both the generated callable and the direct handwritten callable.
A planted generated transition from `Ready` through `Begin` back to `Ready` was refused by that same judge.
Removing the carrier invocation left `GENERATED_TRANSITIONS` absent and produced ordinary Rust `E0425`.
Removing the proc invocation left `State`, `Event`, and `step` absent and produced ordinary Rust `E0433` and `E0425`.
Malformed grammar at `bogus` produced the subject's typed `DeclarationNotRead` diagnostic at that exact token.

The external subject-engine lane also established:

- a missing planned checks role refuses as `MemberMissing`;
- a doubled production role refuses as `MemberDuplicated` with observed count two;
- test-carrier cargo offered to the bench axis refuses as `CargoReachesASecondDestination`;
- cargo proved under another declaration refuses as `RootsDisagree`;
- a lawful rendering reproved against narrowed membership refuses as `MemberUnplanned`.

Repeated identical declarations preserved the terminal expansion, carrier expansion, plan, closure, and explanation identities.
Changing the subject's transition destination moved all five identities.

## Derived expansion reading

The first receipt remains an external projection over existing public readings rather than a new Macroonz type.
Its SHA-256 is `F3D9BAFCBA2BFA658E47DA1366412DC258F590517C6EE942DA1528042D6B7C26`.
An independent parent rerun reproduced that file byte for byte.

The reading names:

- kind `transition-subject.machine`;
- terminal expansion `9478b4f2dfea241eb8eec709e061c7028e6f7dcc3390e3bf7ce4c5fc9793614c`;
- carrier expansion `85688057f3c4e93c6b47d9d0873beb4ffb5860c3b2147b914a4eaf5d27cfc4b3`;
- plan `7394fc886bd5e4b5a068b84e981d667f540ca742cc9119400fdfd14f2be004e8`;
- closure `edad3db09d67feb2ed25487fa39328507fe129bc45938930d02535374d26aecc`;
- explanation `c0b628ce496b89dc6de11a10b9d3916c5a5e12f30a64b101673f97baadd90c78`;
- declaration root `9da7e3e803cac5aa64c52ecae98a2592941eeb7e74f35bd75191f9882016f8b4`;
- content commitment `79fd39a8003ad744e62ae116bd6914201a441daaef0c425904fd5eac51258e82`;
- `production@declaration-site` with digest `befa27e1d71296b7959d2fd504c43437221de9d224a323f2a98c712cb30e6638`;
- `checks@test-carrier` with digest `47eadc869c6949c2ea527f2fafcb1fd19032e849d9a3abc650c8e907c4de8e65`;
- question `finite-transition-denominator`;
- human answer `1 declared transition`;
- canonical answer `0000000000000001`.

## Parent QA correction

The first scratch draft placed seven claim tests behind `#[cfg(test)]` inside the proc-macro library.
Parent review refused that shape because repository law requires external claim lanes and forbids test-only library plumbing.
The accepted shape moved the private subject compiler implementation into `subject-macros/src/engine.rs`, left `subject-macros/src/lib.rs` as proc-host glue only, and externally included the exact engine source from `subject-macros/tests/engine_claims.rs`.
The correction introduced no public testing API, additional proc macro, package, dependency, feature, or product edit.
The externalized shape reran the complete stable command denominator and preserved the derived expansion receipt byte for byte.

## Exact source custody

`source-payload.tar.zst` retains only the authored subject source, manifests, lockfile, toolchain declaration, derived expansion reading, and scratch qualification record.
It contains no compiled target, extracted Macroonz package, package archive, registry cache, or general build output.
The archive uses an explicit sorted file roster, ustar headers, zstd compression, and a fixed `1970-01-01 00:00:00` modification time.
Two independent archive creations produced the same 12291-byte payload with SHA-256 `2D8AFD3F381BC2AF73CCAE250AA8CCB1948B73813CBC4A2186FA59C6F1D3E1AC`.

The payload roster is:

- `Cargo.toml`;
- `Cargo.lock`;
- `rust-toolchain.toml`;
- `subject/Cargo.toml`;
- `subject/src/lib.rs`;
- `subject/src/bin/carrier_removed.rs`;
- `subject/src/bin/macro_removed.rs`;
- `subject/src/bin/malformed.rs`;
- `subject/tests/journey.rs`;
- `subject/tests/planted_wrong.rs`;
- `subject-macros/Cargo.toml`;
- `subject-macros/src/lib.rs`;
- `subject-macros/src/engine.rs`;
- `subject-macros/tests/engine_claims.rs`;
- `evidence/expansion-receipt-v1.txt`;
- `evidence/qualification-run-v1.txt`.

## Disposition

The expansion-receipt candidate remains banked because current `Expansion`, `Plan`, `Closure`, and `View` readings already supplied the complete first consumer denominator.
The relation candidate remains banked because one campaign-authored transition subject does not establish repeated independent ownership pressure.
The subject-local two-expansion `Emittable` bundle remains subject-local because one use does not earn a new host convenience.
The carrier, plan, closure, question, answer, movement, refusal, handwritten, package-only, and renamed-facade observations are accepted for this local Wave B pilot.
The complete Wave B transition journey still owes broader algebra dimensions, further projections and dispositions, independently repeated pressure, mutation, scaling cost, and the later host planes named by the program.

After this receipt and payload are committed and verified, the exact disposable `target/qualification/0.2-wave-b-subject-multiprojection` tree may be cleaned.
