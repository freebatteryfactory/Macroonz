# Macroonz 0.2 compile-contract composition census

This Git-tracked receipt records the composition-before-creation census for the remaining generalized compile-contract obligations.

## Authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Entering HEAD: `637fd43439c69dbfd046319ee950490dc8e5aef7`.
- Exact diagnostic owner receipt: `.durafx/qualification/0.2-a-compile-contract-implementation-20260829/receipt.md`.
- Portable context SHA-256 at assignment: `9A9914CEE4DE4E24ACCB6366D266382A3AF2AB3AF98E2DF0DCE5F6700D3973FE`.
- The census created no scratch, invoked no Cargo command, changed no product source, and crossed no Git-publication boundary.

## Complete read denominator

The bounded census read 136 complete files totaling 22,050 lines and 889,499 bytes plus the assigned 0.2 program sections.
The live denominator covered the complete runner, report, and exact compiled-oracle owners; the relevant Muterprater discovery, interpretation, specimen, verdict, and backend owners; the compiler descriptor and generated-network road; the proc host; facade manifests and reexports; and the existing exact-compiler, runner, report, generated-carrier, proc, facade, and mutation holders.

Search output selected candidates but did not replace the whole-file reads.

## Reuse disposition

The remaining behavior reaches reuse-ladder disposition 1: composition complete.
No new public item, semantic home, package, feature, dependency, facade edge, identity grammar, diagnostic family, runner rail, report rail, or qualification architecture is earned.

- `oracle::compiled` owns exact declarations, observations, comparison, and ordinary oracle conclusions.
- The existing external compiler host owns Cargo and rustc execution, JSON parsing, source-root normalization, and inability to establish an unambiguous anchor.
- `HostTrialRecord` plus runner `record_one` or `record_all` admits the resulting `RunAttempt` under binding-derived `ExecutionKey`, target, toolchain, source revision, check revision, table, and selection standing.
- The facade already exposes the exact contract under `macroonz::harness::oracle` when its existing `harness` posture is enabled.
- The proc host remains capture, span custody, one compiler call, diagnostic placement, and emission.
- The compiler's existing descriptor and network projection road supplies one real generated API without acquiring harness judgment.

## Generated and handwritten contract crossing

The existing `compile_refusals` integration target is the lawful holder for both forms.

The generated subject must be an external package-shaped challenge that aliases the facade, enables the existing `harness` feature, expands `macroonz::macros::network!` through that alias, and consumes the emitted topology and schedule API.
Its lawful and hostile compiler observations must pass through the same `DeclaredCompilation` and `ObservedCompilation` values as the handwritten challenges.
The external host extracts and judges the stable diagnostic anchor; generated source never grades itself.

The exact verdict becomes an ordinary `TrialConclusion`, then an ordinary `RunAttempt::Executed`, then a `HostTrialRecord` admitted through the existing runner join.
Package, feature, target, toolchain, not-run, and infrastructure standing therefore remain outside diagnostic identity and cannot be copied into the oracle.

## Package-candidate choreography

The smallest no-workspace-fallback proof stages all four fresh `.crate` archives under one task-owned scratch root and unpacks the exact archives there.
The external adopter terminates workspace discovery with its own `[workspace]`, declares only a renamed `macroonz` facade dependency, and uses scratch-local `[patch.crates-io]` entries that point to the four unpacked package candidates.

Cargo metadata must prove that every Macroonz package source resolves beneath those unpacked candidates and never through a live repository path.
Archive hashes, unpacked rosters, the generated lockfile, source bytes, commands, target, and feature posture remain qualification inputs.

The minimum local matrix is:

- `x86_64-pc-windows-msvc`, facade defaults disabled and `harness` enabled, for handwritten and generated lawful and hostile contracts;
- the same package posture without `harness`, retained as a typed unavailable or not-run control rather than a subject refusal;
- `wasm32-unknown-unknown`, compile-only with `harness`, for generated API reachability without an execution claim.

This proves local package candidates rather than crates.io delivery.
The current manifests still name version `0.1.0`; no 0.2 registry identity is claimed.

## Muterprater ruling

An expected rustc refusal cannot become successful compiled-specimen pressure.
`CompiledSpecimenHostRefusal::Compilation` truthfully says that no executable artifact existed, so it cannot yield `Meaning` or `CompiledProjectionPressure`.

Compile-contract sensitivity instead remains a separate targeted `cargo-mutants` campaign through the existing wrapped-backend `CompiledSuitePressure` owner.
The existing `trust_opening_evidence` target owns backend-profile qualification, imported output, current-source revision custody, and the distinction between a reported kill and activation evidence.
The campaign must target the exact compiled-oracle comparison, guard, and conclusion source and use the exact compile-contract witnesses as its test command.
It must not be relabeled as specimen pressure.

## Authorized holder-only lot

The next bounded lot may change only existing external holders and retained evidence needed to prove this composition.

- Extend the existing `compile_refusals` holder with the checked runner join and the package-candidate handwritten/generated matrix.
- Reuse the existing compiler JSON parser and scratch choreography rather than adding a process host to product code.
- Run the targeted `cargo-mutants` campaign and import its exact current-source-qualified artifact through the existing `trust_opening_evidence` custody road.
- Retain one readable tracked receipt after independent qualification.

No product implementation movement is authorized by this census.

## Ceiling

This is an ownership and composition result, not execution evidence.
No package candidate, generated challenge, runner join, target matrix, or mutation campaign was executed by the census.
Linux, macOS, hosted, registry-delivered, and runtime Wasm standings remain unproved.
Those facts must be established by the holder-only implementation lot before generalized compile contracts can be called complete on their declared local plane.
