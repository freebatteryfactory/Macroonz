# Wave C architecture-reflexion controls and acceptance blocker

## Standing

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Product baseline: `f9495381e6f6eb95adb278086ed441c15f1d856c`.
- Authority file: `AGENTS.md` at SHA-256 `6DB01AAB69698C926A4CBBD54FD21D086727D46A70BCBEE4EBD145B2A36BC948`.
- Campaign program SHA-256: `30311A0F7D2809A07AB9A3092078ACA2B9A77408836BEEA3657C6C9795A2A147`.
- This receipt qualifies the local Windows Wave C comparison and its controls but does not accept Wave C.
- The exact acceptance blocker is that neither required real subject contains a real missing or forbidden package edge.
- No product source, public API, package, feature, dependency, architecture owner, score, or tracked qualification package was added.

## Declared and observed denominators

Macroonz's declared denominator was parsed from the exact package-arrow fence in `AGENTS.md` section 2.
Its observed denominator was independently derived from locked and offline Cargo metadata and each workspace package manifest.
Cargo metadata and manifest parsing agreed on all six internal edges.
All six declared Macroonz edges were observed, with zero missing or forbidden real edge.

The guarded-transition subject owns its smallest high-level denominator as a typed five-pair constant in `guarded-transition-challenges/src/architecture.rs`.
The disposable driver extracts that subject-owned declaration from stable source and independently extracts the observed graph from the three subject manifests.
All five declared subject edges were observed, with zero missing or forbidden real edge.
The subject declaration does not enumerate private modules, functions, generated units, or implementation edges.

## Readings and controls

The method-specific output exercised `Convergent`, `Divergent`, `Absent`, `Unmapped`, `Unobserved`, and `Inapplicable` without flattening them into a score.
The real Macroonz graph produced six convergent readings.
The real subject graph produced five convergent readings.
A planted missing Macroonz edge and a planted missing subject edge each produced `Absent`.
A planted reverse backedge in each graph produced `Divergent`.
Both planted controls began from nonempty denominators whose selected edge was initially observed.

The Macroonz lawful-variation control uses the existing private `macros/compiler/src/bounded/type_contract.rs` file and a same-home unoccupied `contract.rs` destination while retaining the six-edge package graph.
The driver refuses that control if the source is absent, the destination is occupied, the paths cross semantic homes, or the package denominator is empty.
The guarded-transition lawful variation is the retained direct-versus-refactored renderer pair with the existing exact identity and byte nonmovement witness.

## Capability liveness

The Macroonz compiler-facade capability crossed its declaration, Cargo construction, informed metadata edge, facade holder, external consumer, observable consequence, external witness, and planted hostile reversal.
The guarded-transition capability crossed its declaration, checked construction, informed declaration, generated holder, external consumer, observable consequence, external witness, and retained hostile reversal.
The complete subject workspace was also freshly compiled and executed from the exact previously accepted package archives after the typed architecture declaration was added.
That separate runtime wall passed the complete external journey, historical mutation-custody claim, twenty compiler-side engine claims, all package targets, strict Clippy, and formatting.
The architecture driver retains its narrower source-and-manifest ceiling and does not relabel the separate runtime wall as one of its own extractor readings.

## Exact results

- Macroonz declared edges: `6`.
- Macroonz observed edges: `6`.
- Guarded-transition declared edges: `5`.
- Guarded-transition observed edges: `5`.
- Real Macroonz missing or forbidden edges: `0`.
- Real guarded-transition missing or forbidden edges: `0`.
- Deterministic output lines: `53`.
- Two direct executions were byte-identical after LF normalization.
- Normalized LF UTF-8 output SHA-256: `63C4C4C9BBF2320B9B2F62FB9BD64519FE2F003FEFD6BD6B8528676A06906C46`.

The program's Wave C acceptance sentence requires both Macroonz self-application and one subject repository to report a real missing or forbidden edge.
Because both real graphs are healthy at the selected package-edge denominator, that acceptance sentence is not satisfiable by the observed state.
The planted controls prove that the method detects the required defect classes, but they are retained as planted and are not relabeled as real repository defects.
Wave C therefore remains blocked at its exact acceptance wording while independent later waves may proceed.

## Stable local wall

The driver passed formatting, locked and offline check, strict Clippy, and build under stable Rust 1.98 for `x86_64-pc-windows-msvc` with one Cargo job.
Two direct driver executions each ran locked and offline repository metadata internally and produced identical output.
The subject passed workspace formatting, locked and offline all-target check, strict all-target Clippy, and the complete all-target test denominator under the same toolchain, target, and one-job posture.
The subject package candidates were the exact four archives accepted by the guarded-transition structural-adoption receipt.
Their SHA-256 values were `97D83E3AC53205E9C7C7D090F5C7367E0F0C18857363F2D3F81ACECFDED2598B`, `52855C3B5E7667DBFC8A56C54EBB7E1E54B52195CB1CB1408AB7E0E167A8D64B`, `E0C69BFAE1D7A56A1BE71FBB42FD02BE9CFD6015773A24D6C75A393BC8218A84`, and `5D0C4B2737411ED2B4B9DEEF633DB6BA1F47EED6E028DBEEF0709E05191895D9` for facade, compiler, harness, and proc host respectively.

## Source custody

The accepted guarded-transition source base is `.durafx/qualification/0.2-b-guarded-transition-structural-adoption-20260830/source-payload.tar` at SHA-256 `0682911A9A6EB947248764DA6608C8C4ECD9C3B58A818ADA3FC160CACB340AE1`.
`source-payload.tar` beside this receipt retains the complete disposable driver plus the two-file subject overlay that adds and exposes the typed architecture denominator.
The five retained files are `reflexion-driver/Cargo.toml`, `reflexion-driver/Cargo.lock`, `reflexion-driver/src/main.rs`, `subject-overlay/guarded-transition-challenges/src/architecture.rs`, and `subject-overlay/guarded-transition-challenges/src/lib.rs`.
Their individual SHA-256 values are `D4B30CD77FB39673BA4F6C709EA1D9E220E038ADCD2FB0D800CAEBB3B278DF52`, `4A993C65552710DF08BC498FE8F037841DF12C91F4F11159FA388F640C562E17`, `1ABB3EDA6A0B4843CCFD615C008BA321DB80F04C5F48558595439DC5A710D7FB`, `F6BE225E8C00D328EBE08C40D8EC356541273FF2860BA759637EF8568EEFE996`, and `7D04AE808570029CC93FBA7511440FE201BF583B378CCC5827881AEABE6B5FC1` in that order.
Two independently staged deterministic archive creations produced the same 49,152-byte payload with SHA-256 `E055F2BD0BAE99E140B50CA1F8C183E704105BCF220609D4D9EA40815E7B0EBD`.
The retained source contains no personal identity, absolute host path, lint hatch, unsafe block, build output, extracted package, runtime output, or second architecture file.
Raw Cargo targets, extracted package trees, subject runtime exhaust, custody staging, and the prior-source comparison remain disposable beneath the exact Wave C scratch directory.

## Ceilings and disposition

This is a local Windows x64 stable Rust 1.98 observation.
It does not establish physical Linux or macOS behavior, registry delivery, hosted execution, private-module graph completeness, macro-expansion graph completeness, feature-door completeness, or a universal architecture vocabulary.
The package-edge method deliberately reports private placement and renderer layout as inapplicable and unavailable physical-host claims as unobserved.
The banked public architecture-reflexion overlay remains unearned.
No product change is required by the healthy graph result.
The campaign proceeds to Wave D while this exact Wave C acceptance blocker remains named.
