# Macroonz 0.2 callable-compiler example cure

This Git-tracked receipt records the Q2 documentation example earned by the accepted docs-blind custom-macro census.

## Authority

- Campaign branch: `codex/macroonz-0.2-release-line`.
- Entering census receipt: `.durafx/qualification/0.2-q1-docs-blind-custom-macro-20260829/receipt.md` at `5ff734f`.
- Documentation and example correction: `52729bbdd848b3c96ab3abfbcbd7d642fece285f`.
- Host plane: local Microsoft Windows on `x86_64-pc-windows-msvc`.
- Toolchain: stable Rust 1.98.0 with Cargo 1.98.0.
- This pass performed no public API, dependency, feature, package, proc entry, manifest, lockfile, or product-semantic change.
- This pass performed no push, merge, ref rewrite, publication, or registry mutation.

## Earned correction

The Q1 witness proved that the published compiler already composes a complete subject-owned proc-macro road.
The actor nevertheless had to resolve the public README's undefined subject placeholders across many item pages before it could run one complete callable compiler.

Reuse-first disposition 2 therefore earned one documentation example, not another constructor or semantic owner.

- `macros/compiler/examples/callable_compiler.rs` is one complete 59-line callable compiler inside the existing `macroonz-compiler` package.
- It composes the existing `&'static str` canonical content, `SoleRole`, `NoQuestions`, `Door`, `TextCapture`, token helpers, generated tree, and `Request::render` batteries.
- It reads declared text, plans and renders one declaration-site unit, and asserts the exact emitted Rust token projection.
- It does not enable `host`, call `host::expand`, or claim to execute a proc-macro host crossing.
- `macros/compiler/README.md` adds only a concise route: it labels the schematic adopter-owned placeholders, names the shipped example path and command, and states the callable-only ceiling.
- No implementation body was copied into a README or item doc.
- No item doc was enlarged beyond the code it documents.

## Stable qualification

- `cargo +1.98.0 run -p macroonz-compiler --example callable_compiler --locked` passed and executed the exact token assertion.
- Warnings-denied default and all-feature `cargo +1.98.0 doc -j1 -p macroonz-compiler --no-deps --locked` passed.
- Default and all-feature compiler doctests passed four of four in each posture.
- `cargo +1.98.0 test -p macroonz-compiler --all-targets --all-features --locked` passed every external target, compile-refusal fixture, and example build.
- `cargo +1.98.0 clippy -p macroonz-compiler --all-targets --all-features --locked -- -D warnings` passed.
- `cargo +1.98.0 fmt --all -- --check` and `git diff --check` passed.
- `cargo +1.98.0 package -p macroonz-compiler --locked` packaged and verified successfully.
- The exact README command also passed from Cargo's unpacked post-commit package candidate.
- The coordinator read both changed files completely and independently reran the example, docs, doctests, all-target tests, Clippy, formatting, package, roster, and package-delivered example.

## Package observation

- Released 0.1 compiler archive: 393,098 bytes and 304 entries.
- Post-commit package candidate: 393,904 bytes and 305 entries.
- Post-commit candidate SHA-256: `047502E6892BA3C964CCD16EF0A68AAF29CB88F669A578CDAE52751E27D1691B`.
- The exact roster delta is one path: `macroonz-compiler-0.1.0/examples/callable_compiler.rs`.
- The compressed-byte delta is 806 bytes.
- The owner agent's pre-commit package observation was 393,914 bytes; this receipt records the independently rebuilt committed candidate because VCS metadata changed at commit.

## Disposition

- Reuse-first disposition 2 is closed for the callable-compiler assembly friction.
- The example is a compiled consumer of existing public APIs rather than a second specification or framework primitive.
- No first-party subject macro, new package, public API, dependency, feature, or compiler-harness edge was earned.
- The prior schematic remains conceptual ownership prose, while the one executable file owns the complete callable composition.

## Ceilings

- This receipt proves the ordinary callable compiler road and exact emitted token projection.
- It does not prove `host::expand`, a real proc-macro host crossing, registry-delivered 0.2 source routing, hosted or cross-platform behavior, publication, or release acceptance.
- A public docs.rs source link is intentionally deferred until the 0.2 package containing the example exists and the versioned registry-delivered route can be verified.
