# Macroonz 0.2.0 release receipt

## Standing

- This page is the single receipt for the Macroonz `0.2.0` release plane.
- No `0.2.0` package has been published, no `v0.2.0` tag exists, and no GitHub Release exists at the time this page was opened.
- The first section below records local shape-lot custody that tests cannot retain; every later section appends only after its own hosted, registry, tag, release, or acceptance authority exists.

## Compiled-pressure source custody

The harness mutation lanes read three retained Cargo Mutants console artifacts and compare them with the tracked source they were recorded against.
The source copies that once sat beside those consoles are gone; each console now joins the live tracked source, and the exact revision each campaign ran against is recorded here.

### Current wrapped-backend campaign

- Console: `harness/tests/trust_opening_evidence/current-compiled-pressure-artifact/cargo-mutants-27.0.0-console.txt`, recorded at `5e5bb0a`.
- Source at the campaign: `harness/src/muterprater/backend/wrap/parse.rs`, Git blob `c9275aa93d7bbaca80b3db4e2d5c2cd80cc0541f`, SHA-256 `C9494B4D6570C6E795B1851344D561CE54072653119A049ABDE4DA94AB262FC9`, `4,599` bytes.
- Reconstruction: `git show 5e5bb0a:harness/src/muterprater/backend/wrap/parse.rs`.
- Observation: one selected mutant, `replace != with == in roster_count` at line `68`, column `13`, reported caught by Cargo Mutants `27.0.0` on `x86_64-pc-windows-msvc` under `rustc 1.98.0 (88d9e12ae 2026-08-18)`.
- The campaign was rerun by the shape lot after the wrap road split into its parse, read, and plan roles moved the mutated operation into the parse child; the superseded recording at `bc99da3` stood over `harness/src/muterprater/backend/wrap.rs`, Git blob `53b19ddb28e5e54ac11ed50744d5feb8554fb405`, SHA-256 `EBF016D030A184CDB2879B85FBDAE2648E50CBFF07008CC869C57B0D651CAD4F`, `19,435` bytes, with the same mutant at line `351`, column `13`, and is reconstructed with `git show bc99da3:harness/src/muterprater/backend/wrap.rs`.
- The lane pins the harness-derived revision identity of that source, so an edit to the wrapped backend refuses the campaign join until the campaign is rerun and this section is updated.

### Historical wrapped-backend campaign

- Console: `harness/tests/trust_opening_evidence/compiled-pressure-artifact/cargo-mutants-27.0.0-console.txt`, recorded at `ff0a1b3`.
- Source at the campaign: `harness/src/muterprater/wrap.rs`, Git blob `0ce1fd82b1e2c51018bfc048367f625c86b32f26`, SHA-256 `5E6172E62E5A3B14A1A2F10E672226C5C473A01C7BD652C6FB8F4F11D9B29323`, `19,349` bytes.
- Reconstruction: `git show 40581a0:harness/tests/trust_opening_evidence/compiled-pressure-artifact/wrap.rs`.
- Observation: one selected mutant, `replace != with == in roster_count` at line `348`, column `13`, reported caught by the same backend, target, and toolchain.
- The source coordinate no longer exists in the tree; the lane observes that the historical console still reads, that its coordinate differs from the current one, and that joining it to the current source refuses.

### Compile-contract campaign

- Console: `harness/tests/trust_opening_evidence/compile-contract-pressure-artifact/cargo-mutants-27.0.0-console.txt`, recorded at `4109a6d`.
- Sources at the campaign, all under `harness/src/oracle/compiled/`:
  - `compare.rs`, Git blob `94f49b28c960535e4fd3f0dd8c2dfb627a89d958`, SHA-256 `92E76CC471DE1077CF6D0CEAD2940DAEC4D5FB680A4DEEE7E2E79E10B4700B5D`, `6,474` bytes;
  - `conclude.rs`, Git blob `44b7e8b0f7e5140bdc2b4ca1241e19632d7185ca`, SHA-256 `DD80AE731903821063D4D889FC7E144724B3660C46B555E4C9E065E31BE29ACC`, `2,893` bytes;
  - `type_guard.rs`, Git blob `e6849fa64133c5beef7e69f7d49fd34f313d2c51`, SHA-256 `98A0024FC87B300597BADE99476ABC655B8E8097F57F1EEB6A80B5DF754EEA4E`, `6,841` bytes.
- Reconstruction: `git show 4109a6d:harness/src/oracle/compiled/<file>`.
- Observation: `49` selected mutants, `28` reported caught and `21` unviable, zero survivors, every target owner-unmapped.
- The lane pins the harness-derived revision identity of each source, so an edit to any of the three refuses the campaign join until the campaign is rerun and this section is updated.

## Local shape-lot qualification

### Standing

- Qualified product source: `29fd396b61c6677c040aaae6984092016f18041f`.
- Qualified product tree: `27951501d2e20bccd53a328106cb31ae9cdf4af4`.
- Shape-lot baseline: merge `90bd660190fbeae8f7088d8e55f53c0c0dc8fcee`, tree `4c7b5837199dcfaa29d57ffe35a15249de50ed7a`.
- Scope: `86` commits, `322` changed paths, `17,292` insertions, and `16,616` deletions from baseline through the qualified product source.
- Purpose: ownership, file shape, duplicate-authority removal, test-support custody, and compact evidence custody without a product-behavior change.
- The commit that appends this section is an evidentiary successor to the qualified product source and is excluded from every package archive by the facade's anchored package roster.
- No hosted result, registry delivery, tag, GitHub Release, final acceptance, or packet archive is established here.

### Ownership result

- The compiler recipe road now has distinct capture, account, render, and issue homes behind one unchanged public recipe door.
- Token capture now has distinct cursor, item, and text homes behind one unchanged public token-capture door.
- Kind and identity declarations retain their public roads while lookup, stamping, grammar, and projection operations sit in their semantic operation seats.
- Harness descriptor, proposal, discovery, fuzz, and wrapped-backend responsibilities now sit behind their existing public doors in the operation homes their complete owner reads earned.
- Mixed compiler and harness integration targets now organize their modules by observable claim without adding a Cargo target or a fifth package.
- Shared banks, stamps, bounded operations, test-support helpers, and identity framing were folded only where the current owners asked the same question; deliberate independent oracles remain separate.
- The duplicate wrapped-backend source payloads were removed only after the compiled-pressure reconstruction facts above entered this receipt.
- The final residual census leaves `102` tracked Rust files above the `350`-line routing threshold; the threshold is a reading sensor rather than an ownership verdict.

The move discipline retained the exact Git history as the detailed path map.
Recipe capture, render, issue, and account moved under commits `fec9ed7`, `7cf7c12`, `c958990`, and `b36c918`.
Token cursor, item, and text moved under `fbdae92`, `40dd809`, and `fd425f1`, followed by declaration-seat wiring at `00f4a41`.
Harness descriptor, proposal, discovery, fuzz, and wrapped-backend moves are rooted at `8a6ff5f`, `001e6de`, `41d116d`, `9e38e43`, and `d9b2bcd`.
Every move-only file was Git-recognized as a rename; non-identical moves contain only the module, visibility, import, or reexport wiring recorded by their commits.

### Settled structural exceptions

- Five `TokenKind` partitions remain separate because they answer capture, punctuation, opening-delimiter, closing-delimiter, and current-closer questions rather than one universal classification question.
- The eight-fact benchmark preflight retains its eight-seat constructor because splitting the invariant nucleus would weaken the all-or-nothing declaration boundary.
- The generated-codec behavior claim remains in the existing declaration-roads target because no independent Rust package boundary earns another Cargo target.
- Descriptor length encoding uses the identity substrate, while the existing public refusal variant that lawful construction no longer reaches remains as a compatibility ceiling.

### Mutation result

The exact content-change denominator at `707eb1acee9ea7ec063d81efe3f784f7ecd78585` contained `901` compiler variants:

- `221` caught;
- `678` compiler-unviable;
- `2` demonstrated equivalent;
- `0` viable misses;
- `0` timeouts.

The proc package contributed zero operation-body variants because its changes were moves or declarations rather than function-body changes.
The two equivalents replace `Vec::new()` with `vec![]` for an empty related-material value in `RecipeError::related` and `IntrinsicTextRead::related`.
The token and identity witness commits `d451225` and `707eb1a` promote every meaningful initial survivor into tracked external claims.
The sole later product-source commit changes only the facade manifest's package include patterns, so it adds no Rust operation to the mutation denominator.

### Stable local wall

The qualified product source passed the complete Rust 1.98 local wall on Windows `10.0.26200.0`, `x86_64`, Intel Core i7-8700:

- formatting;
- workspace checking and strict Clippy over every target and feature;
- `661` of `661` ordinary Nextest tests across `75` binaries, with `11` reconciled skips;
- all `5` doctests;
- Cargo Deny advisory, ban, license, and source policy;
- warnings-denied rustdoc;
- the all-feature `wasm32-unknown-unknown` compile posture;
- the stable-rustc coverage example;
- separate compiler, harness, proc, facade, no-default, host, preemption, and all-feature postures;
- every compiler, harness, and proc-host compile-refusal parent.

Six skipped tests are child-process controls invoked by their ordinary parents.
The five intentionally long tests then passed through four explicit commands.
The maximal-recipe witness retained its exact `4,548,871` canonical-byte expectation and finished in `332.27` seconds on this host.
The exhaustive and sampled interleave campaigns finished together in `1.99` seconds, the network campaign in `0.04` seconds, and the longer preemption campaign in `2.54` seconds.

Every Phase A holder ran in the ordinary, long, feature, or extracted-package planes.
The fixed recipe corpus retained its exact generated bytes, canonical identities, readback names, diagnostic contracts, callable-versus-wrapper parity, standard-versus-custom projector parity, renamed-facade behavior, and independent planted-worse controls.
No unexplained public path, supported signature, identity, generated Rust, diagnostic meaning, feature posture, or product behavior moved.

### Qualified package candidates

Rust 1.98 produced these locked offline candidates from the clean qualified product source:

- `macroonz-compiler 0.2.0`: `468` entries, `582,181` bytes, archive SHA-256 `E71B3671F3BF3A2507B3DDC7764FABE1B0AC2A7DADB99FF2F1D0A7296510A178`, LF-roster SHA-256 `772C9A4574D0717BB563AFA51802921117627C85030CA5494D75B8406DD9BA59`.
- `macroonz-harness 0.2.0`: `423` entries, `477,113` bytes, archive SHA-256 `3897701352707F9D3645E7D20D025D1B18745F23588DD75B18FE99552817A8E4`, LF-roster SHA-256 `A2F7BFFA329116E78F65193E69BBB1E1A101FE713C2E405CA9051F297EA67483`.
- `macroonz-macros 0.2.0`: `78` entries, `47,614` bytes, archive SHA-256 `314DA35653D3373F4B7EF11A032E7B6615021CAD50DD74BE1406686FC99C4EDE`, LF-roster SHA-256 `797BCBE088E02BAD746A66019CE3E62BE044AEBD2DFAD9E57945066D8A76E36B`.
- `macroonz 0.2.0`: `25` entries, `136,431` bytes, archive SHA-256 `E1F31B6A07295595AE53D42E6D2573F33540E809AE87C73A0C87E5F94B2255B6`, LF-roster SHA-256 `BB7F2C6392BEC377122BE16FB4E6F484B9F47F64CCD16C52E98826CDC985AFCA`.

Each roster hash covers Cargo's archive path order encoded as UTF-8 with one LF after every path.
Every archive embeds `29fd396b61c6677c040aaae6984092016f18041f` in `.cargo_vcs_info.json` and carries its normalized and original manifests, committed lockfile, owning README, and both licenses.
No archive carries `.durafx`, hosted-workflow material, qualification material, build output, a build script, or the proc fixture package.
The facade roster correction at the qualified source removed two nested READMEs that Cargo's earlier unanchored pattern had admitted.

The four archives were extracted into a standalone workspace that patched crates.io names only to those extracted package directories.
Locked offline all-target and all-feature compilation passed for all four packages and one external facade consumer without a live-worktree fallback.
The external consumer executed a recipe, and the packaged facade recipe, compiler custom-projector, and harness temporal-property examples each executed successfully.
Single-package registry resolution remains unavailable until the four `0.2.0` packages exist in dependency order on crates.io.

### Source and custody census

- Exactly four product packages remain, all at version `0.2.0` on Rust `1.98.0` with the committed exact dependency pins.
- Identity and host-path findings: `0`.
- `allow` and `expect` attributes in Rust source: `0`.
- Python files, build scripts, and symlinks: `0` each.
- Tracked Rust files: `781`, totaling `122,070` physical lines.
- Tracked Rust, Markdown, manifest, workflow, JSON, and text files checked for carriage returns: `899`, all LF-only.
- Macroonz-owned unsafe implementation remains forbidden by the workspace wall; textual `unsafe` occurrences are syntax vocabulary, documentation, and explicit caller-owned boundary specimens.
- Baseline-to-source `git diff --check`: clean.

Exact Cargo-native cleanup removed `19,379` files and `7.8` GiB from the final-wall target, `3,573` files and `2.1` GiB from the extracted-package target, and `19` files from the package-generation target after restoring its standard cache marker.
Four hash-checked candidate archives remain beneath `target/qualification/0.2-shape-lot-20260902/f2/candidates-29fd396` as disposable convenience, not repository authority.
The complete task root retains `399.7` MiB of compact mutation reports, extracted sources, manifests, lockfiles, and candidates rather than compiled output.
The C drive had `17.26` GiB free after cleanup.

### Evidence ceilings and next boundary

- Stable coverage establishes line and region observations, not branch or MC/DC coverage.
- The local Wasm lane is a compile posture, not a browser or runtime observation.
- Source-location restoration matches by spelling rather than exact token origin and can attribute a same-spelled sibling occurrence.
- `cargo-semver-checks 0.48.0` cannot read Rust 1.98 rustdoc format `60`, while `cargo-public-api 0.51.0` requires nightly; neither unavailable tool is relabeled green.
- The hosted workflow remains manual and reporting-only for 0.2.0, and no hosted result exists for the shape-lot source yet.

The next boundary is the owner's editor recheck followed by separate authorization for a complete manual hosted pulse at an explicitly selected revision.
Push, hosted dispatch, merge, registry upload, tag, GitHub Release, final acceptance, and packet archive remain unauthorized.
