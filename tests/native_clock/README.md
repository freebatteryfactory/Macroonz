# Native source crossing

This lane consumes the root native-clock entrance as an ordinary outside caller.
An independently bracketed interval bounds an actual measurement around a host wait, and concurrent callers exercise the shared source through consuming measurement starts.
The harness retains its existing independent synthetic, refusal, unwind and backwards-reading controls.

The controlled-operand module compiles the exact private reading source alongside separately authored expectations for equal, forward and reversed instants and the nanosecond range boundary.
That module observes the subtraction and conversion operations without adding a public observation channel or replacing the native clock.
It does not inject an operating-system clock failure; the public consumer controls separately observe the shipped source and wiring.

The runner control uses inert declared target/toolchain labels and a zero declared time budget while a real subject sleeps and passes.
Changing measurement sources must preserve its execution standing and conclusion; this observes that elapsed measurement does not enforce a deadline.

The target without this adapter observes declared unavailability.
Native host observations do not qualify a different operating system or grant time semantic-verdict authority.
